# records/commitments/
Customer commitment records. One YAML file per commitment.
Schema: `schemas/registry/commitments.v1.schema.json` (to be created in L1-02).
Orphan detection: `commitments[].owner` field checked against people registry.
