# infra/network/ops-vm-shell-gate.yaml
# Generated by access/secrets/tools/emit_shell_gate.py. Do not hand-edit.
# Section 40.3 line 3703 (boundary 2, enforcement E2);
# Section 51.4 lines 4487-4498 (the private network path it sits on top of).
schema_version: 1
boundary: boundary-2-control-plane
enforcement_id: E2
decision: D95
host: "ops-vm-control-plane-01"
private_network_path_required: true
private_network_path_source: "Section 51.4 - every control-plane surface is reachable only over a private network path with SSO in front of it"
private_network_path_is_the_second_factor: false
private_network_path_note: "Section 40.3 says the second factor is ON TOP OF the private path. Treating the path as the factor leaves one factor."
second_factor:
  mechanism: hardware-backed-ssh-key
  key_types_allowed:
    - "sk-ssh-ed25519@openssh.com"
    - "sk-ecdsa-sha2-nistp256@openssh.com"
  touch_required: true
  no_touch_required_permitted: false
  authorized_key_ids:
    - "sk-ssh-ed25519-ops-vm-primary-2026@control-plane"
sshd_required_settings:
  PasswordAuthentication: "no"
  KbdInteractiveAuthentication: "no"
  PermitRootLogin: "no"
  AuthenticationMethods: "publickey"
  PubkeyAuthentication: "yes"
verifier: "infra/network/check-shell-gate.sh"
spec_anchor: "Section 40.3"
spec_lines: "3703"
