# control-plane

Registries, contracts, schemas, validators, reconciler, provisioning, reusable workflows, access and infra config.

`contracts/**` is FROZEN and owned by L0. No lane edits it. See `docs/contract-change-request.md`.
