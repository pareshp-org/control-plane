# L5-98 — DEEP REVIEW, CONSOLIDATED LANE VERDICT

**Scope:** all nine Lane 5 documents — `L5-00-charter.md` (821), `L5-01-org-and-access.md` (5,635), `L5-02-secrets-and-boundaries.md` (5,186), `L5-03-layer-b.md` (3,753), `L5-04-ops-vm.md` (5,143), `L5-05-assets-ai-notify.md` (4,563), `L5-06-tasks.md` (4,568), `L5-07-tests-and-runbook.md` (2,491), `L5-99-review.md` (713). **32,873 lines.**

**Sources merged:** round-1 cross-lane sweeps (21 defects located in L5 files, extracted from `review-harvest/_ALL_DEFECTS.round1-backup.json`) + round-2 per-file reviews (234 defects across eight files). After deduplication: **63 distinct defects — 26 blocking, 25 major, 12 minor.**

**Spot-check date:** 2026-09-02, against the live files and `Research/MultiProduct_MasterSpec_v4.0.md` (10,231 lines).

---

## 1. Spot-checks of the three most serious claims

I re-derived the three highest-consequence claims from the files themselves rather than trusting the reviewers. All three **survive**. Two subsidiary claims elsewhere in the corpus are **REFUTED** and recorded below.

### SPOT-CHECK 1 — CONFIRMED (and worse than reported): `L5-07`'s suite runner recurses on itself forever

`L5-07-tests-and-runbook.md:299` writes the runner to `access/tests/lane5.sh`, then loops `for f in "$d"/*.sh` over `access/tests` — which matches the runner itself — and calls `bash "$f"`.

Reproduced verbatim in a scratch tree:

```
$ timeout 10 bash access/tests/lane5.sh > out.txt 2>&1; echo "exit=$?"
exit=124            # killed by timeout, not terminated on its own
$ wc -l out.txt
387 out.txt         # 387 recursive invocations in 10 seconds
$ head -1 out.txt
--- access/tests/lane5.sh
```

Consequence: `L5-07-01`'s own SELF-VERIFY (`bash access/tests/lane5.sh | tail -n 1` → `LANE5 SUITE OK`) never returns. Sections 5.1, 5.3 and 5.7 of the runbook — the daily gate every subsequent task runs — hang. The lane cannot clear its first task. From T02 onward the runner would also execute `check-coverage.sh` as if it were a test.

### SPOT-CHECK 2 — CONFIRMED: `L5-01`'s fifteen-task chain is dead at task 3

Three independent greps:

```
$ grep -n "gh pr create" lanes/L5-01-org-and-access.md
5529:gh pr create --base integration --head lane/5/p1-integration \      # T15 only

$ grep -c "git rebase" lanes/L5-01-org-and-access.md
0

$ sed -n '546,551p' lanes/L5-01-org-and-access.md
git checkout integration
git pull --ff-only origin integration
test -f access/tools/validate_access_config.py || { echo "MISSING DEPENDENCY L5-01-02"; exit 1; }
git checkout -b lane/5/p1-organisation
```

Every one of T02–T15 cuts its branch from a freshly pulled `integration`, and the dependency guard runs **on `integration`, before the branch cut**. No task in T01–T14 opens a pull request and nothing merges. `validate_access_config.py` exists only on `lane/5/p1-schema-harness`, so T03's guard fires `MISSING DEPENDENCY L5-01-02; exit 1` and every task after it is unreachable. Fourteen such guards exist (lines 550, 858, 1217, 1661, 2182, 2721, 3112, 3466–3468, 3894, 4171–4172, 4650–4651, 5094).

T15's own prose is factually false in two places against its own file: *"Every preceding task pushed its own branch and its own pull request"* (one `gh pr create` exists, T15's) and *"This task rebases on `integration`"* (`git rebase` count = 0).

The identical structure — branch from pristine `integration`, no per-task merge, later tasks writing into directories a predecessor created — governs all 59 tasks of `L5-06-tasks.md`.

### SPOT-CHECK 3 — CONFIRMED: spec line citations are stale corpus-wide, and one task carries three mutually exclusive counts

Section headings resolved by `grep -n '^### <n.n> '`:

| Section | Cited in L5 | Actual heading | Drift |
|---|---|---|---|
| §40.1 | 3644–3654 (L5-00, L5-02) | 3646 | +2 |
| §46.5 | 4179–4181 (L5-00) | 4182 | +3 |
| §51.5 | 4500–4509 (L5-00, L5-02) | 4504 | +4 |
| §64.1 | 5433–5447 (L5-00) | 5440 | +7 |
| §90.2 | 7941–7974 (L5-06 T09) | 7949 | +8 |
| §90.4 | 7990–7999 (L5-06 T14/T49) | 7998 | +8 |
| §90.7 | 8058–8065 (L5-00) | 8066 | +8 |
| §92.11 | 8272–8283 (L5-00, L5-06 T39) | 8281 | +9 |
| §98.2 | 9010–9024 (L5-02) | 9017 | +7 |
| §99.2 | 9184–9231 (L5-00 header) | 9195 | +11 |
| §100.6 | 9421–9441 (L5-06 T47/T55) | 9432 | +11 |

Charter line 8 also states the spec is "10,214 lines"; `wc -l` returns **10,231**.

The §90.2 case is the sharpest, because three numbers disagree inside one task. `L5-06-tasks.md:757` runs `sed -n '7941,7974p' "$SPEC"`; acceptance criterion 1 (`:798`) and the SELF-VERIFY assertion (`:808`) demand exactly 25 rows; the STOP rule (`:821`) forbids editing the assertion — *"recount from the `sed` output"*. Measured:

```
$ awk 'NR>7949 && /^### /{exit} NR>=7949 && /^\|/{n++} END{print n}' spec   -> 29   (header + separator + 27 data rows)
$ sed -n '7941,7974p' spec | grep -c '^|'                                    -> 24   (header + separator + 22 data rows)
```

True transcribable count = **26** (rows 7953–7978; 7979 is the machine-identities row held separately). The `sed` window truncates at 22 and ends mid-table at "Product health". The executor is instructed to trust an output that yields 22, against an assertion that demands 25, when the correct answer is 26.

### REFUTED — two claims that do not survive

**REFUTED — the duplicate `AT-110` rows.** `L5-00-charter.md:653` (escalation E-10), `L5-02:222` (E-P2-09) and `L5-99-review.md:681` all assert §100.6 carries `AT-110` twice, and L5-99 certifies the claim as *"correct"*. It is not:

```
$ grep -n "| AT-110 " spec   -> 9450   (one row)
$ grep -n "^| AT-109 " spec  -> 9449   (one row, well-formed)
```

Spec line 10227 (D113) records the repair explicitly. The escalation is stale and must be withdrawn rather than raised. (The *other* half of E-10 — duplicate `### 46.6` headings — **is real**: 4185 "Monitoring stack unavailable" and 4189 "CI execution estate outage".)

**REFUTED — `L5-99-review.md`'s own headline findings.** Its VERDICT block (lines 11–33) tells a dispatcher that 76 of 152 tasks have no body, that subsystems K and R have no implementation, that 76 task-ids dangle, and that L5-07 has no runbook. Measured heading counts today: L5-00 1 real task, L5-01 15, L5-02 14, L5-03 13, L5-04 19, L5-05 17, L5-06 59, L5-07 14 = **152 of 152 bodies present**; K is built by L5-05-K01..K04 and L5-T24..T32; R by L5-05-R01..R04 and L5-T39..T45; L5-07 §5 is the daily runbook and §8 the blocker templates. L5-99 also certifies the signal catalogue as ending at SIG-46 — the spec has **SIG-47**. **`L5-99-review.md` is a superseded artifact and must not be used as this lane's dispatch gate. This document replaces it.**

---

## 2. Consolidated defect table

Ordered by severity, then by how much execution each defect blocks. `[R1]` marks a defect first raised by a round-1 cross-lane sweep; `[dedup: n]` shows how many per-file reviewers independently reported it.

### BLOCKING

| ID | File | Location | Category | Problem | Fix |
|---|---|---|---|---|---|
| **B-01** | L5-00, L5-01…L5-07 | L5-00 §11.1 `:496-501`; L5-06 all 59 headings; phase-file headings | task-id | **Lane 5 has two complete, incompatible decompositions of itself and seven task-id grammars.** `L5-06-tasks.md` holds 59 tasks (`L5-T01..T59`); the six phase files hold 93 more under `L5-01-Tnn`, `L5-P2-Tnn`, `L5-P3-nn`, `L5-04-Tnn`, `L5-05-Q/K/Rnn`, `L5-07-Tnn`; the charter mandates an eighth (`L5-FF-TT`) that only its own `L5-00-01` uses. Verified: `grep -cE "L5-0[1-7]-T[0-9]" lanes/L5-06-tasks.md` = 0, and the only sibling reference to any `L5-T*` id is a `grep -qF 'L5-T14 SELF-VERIFY PASS'` **string comparison** at L5-07:1406 and :1463 — so any renumber silently breaks L5-07-10's AT-091 leg with nothing to catch it. [dedup: 5 + R1] | **L0 decision.** Pick one decomposition and retire the other; publish a concordance for retired ids. No lane agent may renumber, because of the L5-07 string coupling. |
| **B-02** | L5-06, L5-04, L5-05, L5-03 | L5-06 `:214` vs L5-00 `:683`; L5-04 `:2418/2445/2456` vs L5-05 `:1931/1946/1954`; L5-03 `ops-vm/layer-b/**` vs L5-04 `ops-vm/layerb/**`; L5-05 vs L5-06 `notify/**` | contradiction | **The two decompositions collide on identical file paths, breaking PARTITION rule 3 ("No shared mutable file, ever").** Verified `cat >` collisions: `ops-vm/jobs/run-expiry-check.sh`, `ops-vm/systemd/expiry-check.service`, `.timer` are each written twice with different content (L5-04 uses `WorkingDirectory=/opt/control-plane`, L5-05 `ExecStart=/srv/control-plane/...`) and both `git add` them. Two competing lane-skeleton tasks both declare `Depends on: none` (`lane/5/00-skeleton`, 31 dirs; `lane/5/p0-t01-lane-tree`, 5 dirs). Layer B is built twice under `ops-vm/layer-b/` and `ops-vm/layerb/`. `notify/` has two layouts (one-file-per-route vs `channels.yaml`+`push-events.yaml` registries). | Follows B-01. Whichever decomposition survives, delete the other's writers for every colliding path and name one owner per path in the charter. |
| **B-03** | L5-01 (+L5-06) | L5-01 `:546-551` and 14 further guards; `gh pr create` only at `:5529` | executability | **No branch/merge model, so the dependency chain is dead at task 3.** Every task branches from pristine `integration`, guards its predecessor with `test -f … \|\| exit 1` *before* the branch cut, and no task T01–T14 opens a PR. Same structure across all 59 L5-06 tasks (`git checkout integration && git pull --ff-only` at the head of every one). T15's claims of prior PRs and of rebasing are both false against its own file. **SPOT-CHECK 2 CONFIRMED.** [dedup: 2] | State the model once per file: either each task branches from its predecessor's branch, or a lane-local `lane/5/main` accumulates and only it PRs to `integration`. Replace the `git checkout integration` preamble in all downstream tasks. |
| **B-04** | L5-07 | `:299-315` | command-error | **`access/tests/lane5.sh` executes itself in an unbounded loop.** Reproduced: 387 recursions in 10s, killed by timeout. Blocks L5-07-01's SELF-VERIFY and runbook §5.1/§5.3/§5.7 — the daily gate every later task must clear. **SPOT-CHECK 1 CONFIRMED.** | Add `case "$f" in */lane5.sh\|*/check-coverage.sh\|*/lib/*) continue;; esac`, or move the runner to `access/lane5.sh` and update the six references. |
| **B-05** | L5-07 | `:114-129` (PRE-A…PRE-N) | executability | **Thirteen of fourteen preconditions name paths no Lane 5 file writes.** I greped each literal path across L5-00…L5-06: only PRE-K (`assets/inventory`) resolves. `access/branch-protection/control-plane.yaml` (L5-01 writes `branch-protection.yaml`), `access/codeowners/generate-codeowners.sh` (writes `generate_codeowners.py`), `access/permission-model/teams.yaml` (writes `access/teams/teams.yaml`), `access/secret-tiers/tiers.yaml` (L5-02 writes `access/secrets/tiers/`), `ops-vm/grafana/{shared,founder}/provisioning/` (L5-04 has no split), `notify/routing/push-list.yaml` (L5-05 writes one file per route), `access/ai-runtime/approved-runtimes.yaml`, `ops-vm/org-export/export.sh` (L5-04 writes `ops-vm/export/run-org-export.sh`) — plus PRE-G…PRE-J, which have no producer in any lane. Each is a hard STOP at step 0. [dedup: 2] | Rewrite the table to the exact paths the phase files write, adding the producing task id to each row. Delete or escalate the four with no producer anywhere (see B-14). |
| **B-06** | L5-00, L5-02, L5-06 | L5-00 §4/§5/§7/§9/§13 and header; L5-02 `:200-229`; L5-06 28 `sed -n 'A,Bp'` calls | citation | **Every spec line range past ~line 950 is stale by +2 to +13, and the numbers are baked into executable commands and JSON-Schema `const` values.** Confirmed drift table in §1 above. L5-02's `secret-tier.schema.json` declares `"spec_lines": {"const": "3648-3654"}` (actual 3650–3656) and `fifth-tier-credential.schema.json` `"const": "3683"` (actual 3685), so the wrong numbers become validation-enforced across 15 generated files. Three L5-06 ranges print the *wrong section entirely*: `7990,7999` (§90.4) prints §90.3 plus a bare heading; `8272,8283` (§92.11) stops before the eight-item push list at 8285; `9421,9441` (§100.6) omits AT-095…AT-098, which L5-T55 must transcribe verbatim. **SPOT-CHECK 3 CONFIRMED.** [dedup: 4 + R1] | Re-derive every range mechanically (heading line → next heading − 1), update all of §4/§5/§7/§9/§13, the two schema `const` values, and the "10,214 lines" figure. Add a preflight per transcription task: `sed -n '<start>p' "$SPEC" \| grep -q '^### <n.n>' \|\| exit 1`. |
| **B-07** | L5-06 | `:683-694` (T07), `:757/798/808/821` (T09), `:4290-4306` (T57), `:4483-4507` (T59) | contradiction | **Four tasks ship assertions that fail against the content the same task supplies.** T07's heredoc yields 9 YAML booleans (`required_approving_reviews: 1` is an int, `administrator_exception` a string) and asserts `len(bools)==11`. T09 asserts 25 §90.2 rows where 26 are transcribable and its own `sed` prints 22 (see §1). T57's invariant-108 enforcer names `AT-095; AT-096`, neither of which is one of the eight case ids T55 fixes, so the validator must exit 1. T59's readiness runner invokes two gates specified to exit 1 until a human half runs, while its criterion demands `17/17 PASS`, and its criterion 2 is labelled "fails closed on any gate" with expected output `rc=0`. Each STOP rule forbids the only escape. | Correct each count/assertion to what the supplied artifact actually contains; give T59's LANE-mode expectation explicitly (`15/15 PASS, 2 gates deferred`) and fix criterion 2's label. |
| **B-08** | L5-01 | 11 SELF-VERIFY blocks: `:496, 800, 1162, 2122, 2660, 3412, 3837, 4586, 5034, 5411, 5412`; restores at `:800-802, 1162-1164, 5034-5036` | command-error | **`set -e` aborts every negative test before its result prints, and three tests leave committed files corrupted.** Reproduced: `set -e` / `false >/dev/null 2>&1; echo "B3 $?"` / `echo after` prints only the line before and exits 1 — the `B3` line and everything after never run. Ten of fifteen tasks therefore truncate SELF-VERIFY output and exit non-zero, which the STOP protocol reads as failure. Worse, three tests mutate a committed file and rely on a following `git checkout --` that the abort skips, leaving `stray_key: true` in `organisation.yaml`, three flipped `by_level` rows in `permission-semantics.yaml`, and `control_plane_repository: renovate` in `branch-protection.yaml` in the working tree for the next `git add`. [dedup: 2] | Replace with `if <cmd> >/dev/null 2>&1; then rc=0; else rc=$?; fi; echo "X $rc"`, and move each restore into a `trap … EXIT`. |
| **B-09** | L5-02, L5-03 | L5-02 `:40-95`; L5-03 `:1846-1851, 2077, 2843-2847` | executability | **`contracts/access/access-inputs.yaml` — the single frozen input all fourteen L5-02 tasks and three L5-03 tasks read — is created by no task anywhere.** Verified: `grep -c access-inputs lanes/L0-01-phase-0-contracts.md` = 0; the only `cat >` occurrences are L5-02's own test fixtures. L5-02's preflight prints `INPUT-MISSING` and stops the phase before T01. Compounding: L5-02 §2 declares the file's shape exhaustively (`schema_version`, `secret_name_registry`, `fifth_tier`, `ops_vm`, `checks`) with **no `layer_b` key**, while L5-03 reads six `layer_b.*` keys from it — two files in one lane give one frozen contract two incompatible shapes, so L5-P3-08/09/11 STOP and the People-tier gate (L5-P3-12) is unreachable. [dedup: 2] | L0 authors the contract in Phase 0 with the union of both shapes, and both files cite the same §2 block. |
| **B-10** | L5-00 | §7 `:288`; §10 DoD-04, DoD-06…DoD-15 `:445-459`; §2.1 `:55-78` | completeness | **Eleven of sixteen Definition-of-Done rows are provable only by `l5-validate.sh` modes no task implements, and eight of the 31 fixed directories have no writer.** Verified: `grep -rc l5-validate lanes/L5-*.md` returns hits only in `L5-00-charter.md` (26) and the superseded `L5-99-review.md` (4) — **zero in every phase file**, so `--coverage`, `--publish`, `--schema`, `--fail-closed`, `--assets`, `--notify`, `--pins`, `--secrets`, `--machine-identity`, `--nondelegable`, `--plane-split` all fall through to `UNIMPLEMENTED-MODE; exit 2`. Separately `access/ai-runtime/{runtimes,extensions,mcp-servers,constitution}`, `infra/github-org`, `infra/published`, `ops-vm/smoke`, `ops-vm/published` are mentioned in **zero** phase files, yet `--layout` only tests `[ -d "$d" ]` — so DoD-01 returns `L5 LAYOUT PASS` over eight permanently empty trees that carry §7's publication promises. [dedup: 3] | Assign each mode to the task that authors the data it validates; assign each of the eight directories a producing task or delete it from §2.1, the mkdir loop, the `l5_dirs()` heredoc and §7. Until then mark DoD-04 and DoD-06…DoD-15 blocked. |
| **B-11** | L5-00 | §7 `:288-306` vs L5-01 §0.3 `:36-40`; L5-05 §5 `:4496` | contradiction | **Only 4 of the 15 declared publication artifacts have a producer, and the two most-consumed have none.** Charter rule P-1 is absolute — *"A consumer reads `*/published/**` only. Reading any other L5 path is a cross-lane violation."* L5-01 publishes neither `access/published/permission-model.v1.json` (which D provisions from and C reconciles against, SIG-03) nor `access/published/branch-protection-profile.v1.json`; instead §0.3 tells L3 to reach into `access/codeowners/generate_codeowners.py` and `access/tools/render_protection_payload.py` — non-published paths, i.e. the violation P-1 defines. `assets/published/asset-owners.v1.json` is recorded in L5-05 §5 as an existing output that L3's orphan detection consumes, and is written by no task (single mention corpus-wide). [dedup: 3] | Add the producing tasks, or move the consumers' contract to what L5 actually publishes. Fix charter §13 E-01, which says "ten publication artifacts" where §7 defines fifteen. |
| **B-12** | L5-01 | `:362-370, 404-410` (`validate_access_config.py`) vs L5-02/03/05/06/07 | contradiction | **L5-01's validator treats every sibling's YAML under `access/` as a failure.** It globs `access/**/*.yaml`, excludes only `access/testdata/`, and appends `unschemad access document: %s` as a FAILURE for anything no `x-target` names. Siblings prescribe ~30 such documents (`access/access-inputs.yaml`, `access/fail-closed/*.yaml`, `access/layer-b/*.yaml`, `access/ai-toolchain/runtimes/*.yaml`, `access/model/*.yaml`, `access/tests/coverage.yaml`). Once any merges, every `ACCESS CONFIG VALIDATION PASSED (N documents)` criterion in L5-01 — C1, D1, E1, F1, G1, H1, I1, J1, K1, M1, N1, O2 — is permanently unsatisfiable. | Scope `CONFIG_GLOB` to the directories L5-01 owns, or make an unschema'd document a warning plus an explicit allowlist of L5-01-governed roots. |
| **B-13** | L5-03, L5-05, L5-06 | L5-03 `:1353, 1379, 2651, 2820`; L5-05 `:233, 355`; L5-06 `:2690-2731, 2757, 2803` | contradiction | **`assets/inventory/` has three incompatible schemas and two filename conventions inside one lane.** L5-05's closed `asset_class` set is `machine_credential, encryption_key, host, ci_runner, ai_subscription_seat, detection_leg, certificate, domain, oauth_credential, signing_certificate, vendor_contract, founder_account, intake_channel`; L5-03 writes four entries with `control-plane-machine-credential`, `encryption-key`, `registered-key-material`, `accepted-risk` (verified by grep) and none of L5-05's five other required fields. Every `ASSET-VALIDATE: PASS (N files)` criterion in L5-05 — Q02 c3, Q03 c5, Q04 c3, Q05 c4, Q06 c2, Q08 c1 and the §6 DoD — becomes unreachable once L5-03 merges. L5-06 declares the directory holds "exactly 11 files" under a third naming scheme with `additionalProperties: false` and a top-level key of `id` where L5-03 uses `asset_id`. The Layer B backup credential gets two entries under two names, so the §49 expiry sweep double-counts it. [dedup: 3] | L0 fixes one entry schema, one `id` key and one filename set for `assets/inventory/**`, applied to L5-03, L5-05 and L5-06 in a single change. |
| **B-14** | L5-07, L5-04 | L5-07 `:122-125, 191-192, 2492`; L5-04 `:2853` | path-ownership | **Lane 5 builds artifacts for subsystems J and O, which PARTITION v1 assigns to no lane.** PRE-G…PRE-J and tasks T06/T07 build tests for `infra/hermes/*.yaml`, `infra/egress/background-host-allowlist.conf`, `infra/systemd/background-window-stop.service` — all subsystem J in spec §99.2's tool table. L5-07 §3.3 relabels AT-108/AT-109 as "K, M" and "K", so the claim is invisible, while its own §10 rule 6 forbids exactly this. L5-04-13 separately routes an audit-log accepted-risk record to subsystem O. The unassigned-subsystem question is open under **five conflicting ids** across the corpus (LA-01, D-REQ-4, DECISION 1, D-EE-2, DEC-01) plus a sixth here (L5-03 E-P3-01), none cross-referencing the others. [R1, dedup: 3] | L0 rules on G/H/J/O/P once, under one id, and records it in PARTITION. Until then mark L5-07-06/T07 and L5-04-13's O leg blocked and correct the mislabelled Subsystem column. |
| **B-15** | L5-03, L5-06 | L5-03 `:62`; L5-06 `:94` | command-error | **The blocker-issue template every STOP rule routes through cannot run.** Both derive the repo slug with `sed -E 's#.*[:/]([^/]+/[^/]+?)(\.git)?$#\1#'`. POSIX ERE has no lazy quantifier: `[^/]+?` parses as greedy `[^/]+` followed by optional-empty `?`, so `(\.git)?` matches empty and the result keeps the suffix — `gh issue create --repo acme/control-plane.git` fails with "could not resolve to a Repository". Every escalation in both files is inoperable. Compounding across the lane: eight `gh issue create` calls in L5-06 and five in L5-07 pass labels (`blocker`, `lane-5`, `assisted`, `l0`, `handoff`, `l2`, `human-action`, `security`, `contract-change-request`) that no task creates — `gh` errors out on an unknown label. [dedup: 2] | Use `--repo "$(gh repo view --json nameWithOwner -q .nameWithOwner)"`. Add a Phase-0 `gh label create` step with its own SELF-VERIFY. |
| **B-16** | L5-00, L5-01, L5-02, L5-03, L5-05, L5-06, L5-07 | L5-00 11×`$CONTROL_PLANE_ROOT`; L5-01 `:129`; L5-02 `:78`; L5-03 `:42`; L5-05 `:34`; L5-06 `$REPO_ROOT` | executability | **Five names for the working root, four incompatible values, one never assigned, and no clone step.** Measured: `CONTROL_PLANE_ROOT` 11 uses in the charter and **0 assignments anywhere in Lane 5**; `CP_ROOT="$PWD"` (L5-01, 15×); `CP_ROOT=/c/D_Drive/PS/MultiProduct/control-plane` (L5-02, 3× — an operator-specific absolute path); `CP="$HOME/work/control-plane"` (L5-03, L5-05); `$REPO_ROOT` (L5-06, 63 uses); L5-07 uses none. The blocks do not run under `set -u`, so `cd ""` is a silent no-op: the charter's `L5-00-01` mkdir loop creates 31 directories and `git add access infra ops-vm notify assets` in whatever directory the shell happened to be in. `$CONTROL_PLANE_REMOTE` (L5-03 `:135`, the lane's only clone) is defined nowhere in the corpus. [dedup: 5] | One root convention for the lane (recommend `CP_ROOT="$(git rev-parse --show-toplevel)"` at the head of every block), a literal clone step in a Phase-0 task, and `: "${CP_ROOT:?}"` guards so an unset root fails loudly. |
| **B-17** | L5-00 | `L5-00-01` `:776-819`; L5-05 all 17 tasks; L5-07 all 14 tasks | missing-control | **SELF-VERIFY runs after `git commit`/`git push`/`gh pr create`, so no STOP rule it gates can fire.** In the charter three of five assertions read `origin/integration...HEAD`, which is empty pre-commit, so SELF-VERIFY *deterministically* prints FAIL on a correctly executed task — while the STOP rule forbids committing when it fails, and the commit, push and PR the rule forbids have already happened. In L5-05 fourteen STOP bullets (`Do not commit`, `git rm …; amend, then push`) are unreachable, and the fixtures they warn about are created only inside SELF-VERIFY, i.e. after the commit. L5-07 has the same ordering in all fourteen tasks. [dedup: 3] | Reorder every task: write files → SELF-VERIFY (+ foreign-path guard) → gated `git add`/`commit`/`push`/`pr`. Split the charter's block so `git commit` precedes SELF-VERIFY and push/PR follow it. |
| **B-18** | L5-02 | `:3283-3288, 3324-3325, 3345, 3358` vs L5-04 `:411-413, 1493, 1548-1549` | contradiction | **L5-02's host verifier can never pass against the host L5-04 provisions, and it is declared the verifier phase 4 must satisfy (HO-07, X-09 requires `RESULT PASS`).** Three permanent failures: OWNER — L5-02 expects `cp-creds`, L5-04 runs `useradd --system … ops-vm` and `install -d -o ops-vm`; DELIVERY — L5-02 requires `LoadCredential=\|SetCredential=` in the unit, L5-04 delivers with `set -a; . ops-vm/credentials/store.env` and contains no `LoadCredential` anywhere in the lane; AUDIT — L5-02 requires an auditctl rule on `/var/lib/ops-vm/creds` that no L5 file installs. The four values causing this (`store_path`, `owner_os_user`, `directory_mode`, `file_mode`) are hard-coded in L5-02 despite its own promise that every judgement value comes from the frozen contract. | Reconcile in one L0 decision: one OS user, one delivery mechanism, and an auditd task for `/var/lib/ops-vm/creds`; move the four values into `access-inputs.yaml` and have both phases render from it. |
| **B-19** | L5-04 | `:1190, 1213, 1231` | command-error | **`layerb-instance-separate.sh` can never print PASS.** `grep -q 'people' ops-vm/grafana/provisioning/datasources/layer-a.yaml && { echo "people datasource in shared instance"; fail=1; }` — and `layer-a.yaml`, written by T04 at `:840`, contains the literal comment *"The people datasource is NEVER registered here"*. Verified by reading both. The check always FAILs; and because the script runs `set -euo pipefail`, the non-matching branch aborts the script on the failed `&&` list before the verdict, so it exits 1 either way. AC #2 and the SELF-VERIFY both demand `LAYERB-INSTANCE-SEPARATE: PASS`. | `if grep -v '^ *#' layer-a.yaml \| grep -qE '^ *- name: (people|layer-?b)'; then … fi`. |
| **B-20** | L5-04 | `:855` (`layer-a.yaml`), consumed at `:3777` | executability | **The shared Grafana datasource ships unexpanded `${DEVLAKE_RO_USER}`/`${DEVLAKE_RO_PASSWORD}`, so every post-patch smoke run fails and every patch is reverted.** Both variables appear exactly once each in the whole file — at their point of use. They are in no `.example`, no `require_env`, and nothing renders the file: it is not a `.tmpl`, and `30-stack-up.sh` renders only `prometheus.yml.tmpl`. `grafana-provisioned.sh` then fails every datasource health check, so SMOKE-1/SMOKE-2 always fail and `patch-driver.sh` reverts unconditionally. | Rename to `layer-a.yaml.tmpl`, add both keys to a `grafana.env.example`, and add the `envsubst` step to `30-stack-up.sh`. |
| **B-21** | L5-04 | `:1393, 1932, 2145, 3063` (timers); `:4030-4051` (steps.yaml R06–R15); `:412-413` vs `:1571, 3040, 1637, 3117, 3369` | command-error | **The operations VM cannot be rebuilt as written — three independent stops.** (a) Four `.timer` units are written with `OnCalendar=${LAYERB_BACKUP_ONCALENDAR}` etc.; systemd does not expand shell variables in unit files and nothing renders them, so `systemctl enable --now` fails with "Failed to parse calendar specification". (b) `steps.yaml` runs `systemctl enable --now <name>.timer` at R06–R15 but **no step ever installs the unit files** into `/etc/systemd/system/` or runs `daemon-reload` — the rebuild aborts at R06, defeating §45.4's 4-hour clock. (c) Five checks and two job wrappers write to `/var/lib/ops-vm/metrics/`, which `00-base.sh` never creates (`grep -c 'install -d.*metrics'` = 0); under `set -euo pipefail` the redirect aborts the job after a successful reconcile, so `reconcile-freshness.sh` reports `NO-RUN-RECORDED → LEVEL-5` permanently. | Rename the four to `.timer.tmpl` and add a `50-timers.sh` envsubst step; insert an `install … /etc/systemd/system/ && systemctl daemon-reload` step before R06; add `metrics`, `health`, `inventory`, `rebuild`, `restore-rotation` to `00-base.sh`'s `install -d`. |
| **B-22** | L5-03 | `:825, 918-919, 3064, 3205` | contradiction | **The People-tier gate turns GREEN on evidence never examined.** AT-097, AT-089 path 5 and the invariant-109 clause-1 check all scan `ops-vm/shared/provisioning/**`; `grep -rn 'ops-vm/shared'` matches only this file — the shared Grafana tree is at `ops-vm/grafana/provisioning/` (L5-04 `:821-834`). Every grep runs against a non-existent directory, prints nothing and reports clean; the AT-097 script additionally wraps both repository checks in `if [ -d "$SHARED_PROV/datasources" ]`, so `FAILS` stays 0 and it prints `AT-097_PASS`. AT-097 is one of the four tests §98.6 names in the definition of "verified". §90.4's own warning applies verbatim: *"A check scoped narrower than the access it exists to verify reports clean for the wrong reason."* | Repoint to `ops-vm/grafana/provisioning`, and replace the `if [ -d ]` guards with `\|\| { echo AT-097_INDETERMINATE; exit 2; }` so absence fails the gate. |
| **B-23** | L5-03 | `:1037` | command-error | **The "idempotent" volume script reformats the live Layer B store.** `sudo cryptsetup luksFormat … '$DEV' \|\| true` runs unconditionally whenever `/dev/mapper/$MAP` is absent — which it is after every reboot — destroying every person's evidence, then prints `PROVISIONED` because `\|\| true` swallows the failure. The `$(yq -r '.volume.cipher' /dev/null …)` substitution reads `/dev/null` instead of the declaration, so the declared cipher is decorative, and `luksFormat` is interactive while `ssh` is called without `-t`. | Gate on `cryptsetup isLuks` (never format a formatted device), read the cipher from `$DECL` before the ssh call, drop `\|\| true`, use `--batch-mode` with an explicit passphrase route. |
| **B-24** | L5-03 | `:2125-2146, 2231, 2248, 2294`; `:522` | command-error | **The allowlist sync deletes the Grafana instance's own admin, and four gate-critical checks are unpassable.** `HAVE` is the full `/api/org/users` list, which always includes `layerb-admin` (created because `grafana.ini` sets `admin_user = layerb-admin`), and `layerb-admin` is never a `people-intelligence` holder — so `DEL` always contains it and the non-dry-run sync issues `DELETE /api/admin/users/<id>` against the instance admin, locking the instance out; SCOPE-2 always emits `PARITY_BLOCKING layerb-admin`; AT-091 test (d) can never see `add=0 remove=0`; AT-092's diff can never be empty. Separately, `check-allowlist-parity.sh` reads an accepted-risk file written by L5-P3-11, which L5-P3-09 does not depend on, so in index order the legitimate host admin is flagged Blocking on a correct estate. And criterion 9 (`git grep -c 'people_intelligence_delegate' … \| wc -l` → `0`) is guaranteed to return 2, because this phase commits that token twice by design. | Exclude `admin_user` from both sets and refuse to delete it unconditionally; add `L5-P3-11` to L5-P3-09's Depends-on; scope criterion 9 to exclude the two files that legitimately carry the token. |
| **B-25** | L5-05 | `:634-641, 800-802, 1114-1115` + Q04/Q06 SELF-VERIFY; L1-01 `:141`; PARTITION `:39` | command-error | **The owner resolver's exit code is discarded and its failure text is written into six committed YAML files.** `DEVOPS="$(python3 assets/resolve_holder.py capability:devops \| sed 's/^RESOLVE: //')"` runs with no `set -e` and pipes through `sed`, so the pipeline status is always 0 and the prose-only STOP rule never fires; on failure `$DEVOPS` becomes `NONE (no active holder of capability:devops)` and lands as `owner:`. This is not hypothetical: L1-01-09 seeds `registries/people.yaml` **empty and schema-valid**, and PARTITION `:39` merges L1 before L5, so the resolver prints `RESOLVE: NONE`, exit 2, on every call and `validate_assets.py` raises R8 on every entry. | `DEVOPS=$(python3 …) \|\| { echo "STOP: $DEVOPS"; exit 1; }; DEVOPS=${DEVOPS#RESOLVE: }`. L0 states whether L5 Phase 5 may run before `people.yaml` is populated. |
| **B-26** | L5-07 | `:2217, 2236-2246, 2361-2363` | contradiction | **The runbook blocks every commit and every working day in the lane.** §5.1 makes `LANE5 SUITE FAIL` at start of day a STOP ("open a blocker before starting any new task"), but `lane5.sh` sets RC=1 on any non-zero child and every ASSISTED test is *designed* to exit 2 until a human returns evidence — so from the moment T05 merges, every day begins with a false blocker. §5.3 requires `COVERAGE OK` before every commit, but `check-coverage.sh` fails for every manifest row whose test file is not yet written — on T02's own branch all 32 are missing, so no commit in the lane is legal until all fourteen tasks have merged. §6 then declares `REVALIDATION STALE=0` the only state in which Phase 1 may be reported met, while the fourteen AUTO checks carry no evidence file and each increments STALE — the gate is unreachable by construction. | Make `lane5.sh` distinguish rc=2 (INCOMPLETE) from rc=1; gate the manifest→file direction of coverage behind `--strict` used only at lane close; filter the revalidation sweep to `mode != AUTO`. |

### MAJOR

| ID | File | Location | Category | Problem | Fix |
|---|---|---|---|---|---|
| M-01 | L5-04 | `:1568` vs L3-01 `:7162-7169` | contradiction | **[R1]** L5's systemd wrapper invokes `reconcile --mode "$MODE" --emit-metrics …`; L3's CLI defines only `--root`, `--org` (required), `--out`, `--external-cause`, `--acknowledgements`. Two unknown flags plus a missing required flag → argparse exits 2 on every scheduled run. | L0 freezes one reconciler CLI contract; L5's wrapper and L3's argparse are edited in the same change. |
| M-02 | L5-01 | `:2296-2320` vs L0-01 C-WF-CHECKS-1 | contradiction | **[R1]** Four independently authored required-status-check lists at five paths, two of which each call themselves the single source. `tests` and `reviewer-matrix-validation` are required by L5's branch protection and compared by L3's CMP-05 but emitted by no L2 workflow. | L0 freezes one context list; L5-01-07, L2 and L3 all cite it. |
| M-03 | L5-06 | `:353-398` (L5-T04) | contradiction | **[R1]** `check_contract_inputs.py` fail-closes on five declared inputs (`contracts/people.schema.json`, `contracts/platform.event-types.json`, …), every one a rename of a differently-named frozen L0 contract. The gate blocks the lane on filenames that do not exist. | Cite the frozen names verbatim. |
| M-04 | L5-07, L5-02 | L5-07 `:1999-2178` vs L2-05 `:3417-3489`; L5-07 vs L3-07 `:1020-1112` | contradiction | **[R1]** Seven acceptance tests are built twice by two lanes with incompatible assertion counts and separate evidence stores, with no cross-reference (AT-029: L2 `checked=3`, L5 a four-leg human-action card; AT-035 likewise). | L0 assigns one owner per AT id and deletes the duplicate. |
| M-05 | L5-07 | `:203` vs protocol/00 §6, protocol/02 `:231-237`, master/05 L5P5-E5/E6 | contradiction | **[R1]** L5-07 states verbatim that AT-093, AT-094, AT-095 and AT-099 are out of Lane 5's scope; all three ownership maps assign them to L5, and master/05's exit criteria require them. | L0 reconciles the four maps. |
| M-06 | L5-00…L5-99 | protocol/05 IG-02 / LG-05 | missing-control | **[R1]** `grep -rc -i 'seeded.defect' lanes/L5-*.md` = 0 across all nine files, but IG-02 requires `seeded_defects=5/5 DETECTED` and LG-05 requires `seeded_defect=DETECTED` on every lane PR. GATE B cannot pass for L5. | Add L5's seeded defect, or L0 amends the gate. |
| M-07 | whole lane | 42 `chmod +x` in L5-04, 8 in L5-02, T01 in L5-00, L5-03, L5-06 `bc`, MSYS `/tmp` in L5-02, `yq` flavour in L5-03/L5-07 | executability | **Windows/Git Bash portability, five distinct failures.** `core.fileMode` defaults false, so every `chmod +x` is lost from the commit and every `ExecStart=/opt/control-plane/…sh` gets EACCES on the Linux VM — while SELF-VERIFYs that `test -x` pass locally, certifying a bit that was never pushed. `bc` is **absent on this operator's machine** (verified: `command -v bc` → nothing), so L5-06-34's `paste -sd+ \| bc` dies. MSYS argv conversion rewrites leading `/tmp/...` paths, so three character-exact expected outputs in L5-02 can never match. `yq` flavour/version is unpinned while the corpus mixes kislyuk `-r` syntax with mikefarah `-i`/`del()`. | Add `git update-index --chmod=+x` after every `chmod`; replace `bc` with `awk`; move `/tmp` fixtures into repo-relative scratch; pin the `yq` flavour with a version probe in each file's preflight. |
| M-08 | L5-07, L5-02, L5-03, L5-04 | L5-07 `:730-750`; L5-02 `:3341`; L5-03 `:823-856`; L5-04 `:3096-3103` | missing-control | **Six controls fail open in a lane whose own doctrine forbids it.** NC-06 (`gh api … \|\| echo '[]'`) passes vacuously with no org, no auth or a rate limit — the check that proves "NO machine bypass actor (D89)". H3's `find … 2>/dev/null` gets EACCES on a 0700 directory owned by another user and reports `no-loose-modes` having inspected nothing. AT-097 reports PASS over an absent directory (see B-22). `export-no-secret-values.sh` uses `grep -RIl`, and `-I` skips binaries — so the only file that could carry secrets, the `.tar.gz`, is skipped by construction. | Capture exit statuses and exit 2 on any unreadable input; scan the extracted archive; replace `\|\| echo` fallbacks with INDETERMINATE branches. |
| M-09 | L5-06 | 23 SELF-VERIFY blocks (`:683, 1006, 2074, 2197, 2416, 2789, 3086, 3417, 3593, 3887, 4189, …`) | missing-control | Twenty-three tasks' primary gate is "run the tool you just wrote and check it prints the exact success string this task told you to make it print". A tool whose entire body is `print('…'); sys.exit(0)` satisfies the gate, the acceptance table and the readiness runner. The eight tasks that resist this (T07, T08, T09, T16, T37, T39, T49, T54) assert properties of the committed artifact instead. | Add to each a second assertion over the artifact (inline python/grep the tool cannot influence) plus one negative fixture that must exit 1. |
| M-10 | L5-06, L5-02, L5-01, L5-05 | L5-06 twenty `pytest … \| tail -1`, 13 undefined CLI flags, 3 missing fixtures; L5-02 16 `grep -c` rows; L5-01 `:4581`; L5-05 `:2833` | executability | **Unmatchable or self-satisfying acceptance criteria.** `pytest -q`'s last line is `14 passed in 0.12s`, never the declared `14 passed` — twenty "Expected output (exact)" blocks cannot match. Thirteen acceptance commands pass flags (`--contract`, `--dir`, `--people`, `--matrix`, `--verify-verbatim`, …) that the tool's own "no additional behaviour" contract never defines. Three read fixtures no task creates. Sixteen L5-02 criteria expect `0` from a `grep -c` whose zero-match case exits 1. L5-01-12's `bash -n … \|\| echo BAD; echo SYNTAX-OK` prints the success token unconditionally. L5-05-K03's `PATH=/nonexistent bash script` yields rc=127, not the declared rc=3, because `bash` itself is not found. | Fix each expected string to what the command emits; add every used flag to its tool contract; create the three fixtures; append `\|\| true` or restate as `test "$(…)" = 0`. |
| M-11 | L5-03, L5-05, L5-01, L5-04 | L5-03 `:1229-1236, 1244, 507, 781-787`; L5-05 `:776-830, 1120`; L5-01 `:3934, 4033, 2296-2320`; L5-04 `:1647, 3124` | judgment-required | **Calibrated and operational values are invented in-lane, against the charter's own E-09 escalation.** L5-03 sets `object_lock_mode: COMPLIANCE` with `retention_days: 3650` — an irreversible ten-year lock, the most consequential single value in the phase — plus run-count ceilings, key rotation cadence, token rotation interval, a pinned Grafana image and a datasource endpoint. L5-05-Q03/Q05 fabricate `site: primary-site`, `power`, `network`, `hostname`, `machine_account`, `cost_band` for two hosts — while the same file's Q04/Q07 STOP rules forbid exactly that ("inventing one puts a fabricated deadline into the watch") and its §6 claims "It shipped no roster". L5-01 freezes `seal_audit_month_of_quarter: 2` with a JSON-Schema `const`, so the recalibration the spec mandates would fail validation, and assigns per-context `added_at_phase` values that charter E-07 reserves for L0. L5-04 invents a 24-hour Level-5 threshold §51.2 does not define, and grades the org export Red one day after success against a 90-day window. | Move every one into the frozen contract with a STOP-if-absent rule, or route to L0 as calibrated configuration. Do not let a lane freeze a calibrated value with a schema `const`. |
| M-12 | L5-00, L5-01 | L5-00 §11.1 `:500`, §11.3 `:505-560`, §11.4 rules 5–6, §12 rule 6 `:630` | contradiction | The charter's mandatory task template is followed by nothing. Branch grammar: §12 rule 6 says `lane/5/<phase>-<task>`, §11.3 orders `lane/5/FF-<slug>`, the charter's own task uses `lane/5/00-skeleton`, L5-04 uses `lane/5/04-t01-layout`, L5-06 `lane/5/p0-t01-lane-tree`, L5-01 `lane/5/p1-<slug>`. The template requires `**Subsystem:**` and `**Spec:** §n.n (lines A-B)` fields no phase task carries, and rule 5 ("SELF-VERIFY prints exactly one line") is broken by every SELF-VERIFY in the lane. `<slug>` is itself a placeholder §11.4 rule 1 forbids. | Amend §11 to the form the 152 bodies actually use, or reissue. One decision, applied lane-wide. |
| M-13 | L5-04 | §1.2 `:86-89` and 19 commit blocks (`:331, 528, 758, 981, 1198, 1375, 1655, 1806, 1918, 2170, 2503, 2757, 3141, 3412, 3931, 4234, 4467, 4759, 4900`) | missing-control | The lane guard is `git diff --cached --name-only \| grep -Ev '…' && echo "FOREIGN PATH STAGED - STOP" \|\| echo "PATHS OK"` followed, in the same block, by an unconditional `git commit` and `git push`. No `exit`, no `&&` chain, no `set -e`. The sole in-file control protecting PARTITION rule 1 does nothing. §1.2 also describes `git status --porcelain` while the command inspects `git diff --cached`. | Hard gate in its own block: `if git diff --cached --name-only \| grep -Eqv '…'; then echo STOP; exit 1; fi`. |
| M-14 | L5-06, L5-05 | L5-06 `:3036-3060, 3121-3155`; L5-05 `:909`; PARTITION `:27` | contradiction | PARTITION rule 3 ("directory-per-item only; no lane appends to a shared index") is violated by prescription. L5-06 ships `notify/contract/push-events.yaml` (all eight events) and `notify/routing/channels.yaml` (every channel) — registries-of-everything; L5-05 implements the opposite and correct reading, one file per route. L5-05 separately ships `infra/runners/hosts.list` whose committed header is literally an append instruction, against its own §0 rule B-5. | L0 either restates rule 3's scope or splits all three into directories; update every dependent count assertion. |
| M-15 | L5-05, L5-06, L5-07 | L5-05 `:3543-3600` + P7; L5-06 `:3033`; L5-07 `:1731` | citation | Every `event_type` identifier in the lane is coined, not cited. §97.3 publishes the taxonomy as prose and says the enum is declared in `platform.yaml` — L1's file. Two coinages also break §97.3's one-identifier-per-entry rule (`verification_blocked` + `verification_unblocked` from one entry). Because L1 merges before L5, `check_push_list.py` will run in ACTIVE mode and P7 fires on every identifier L1 spelled differently — eleven simultaneous blocker issues as the only defined outcome. | L0 freezes the eleven identifiers in `contracts/` in Phase 0; L1's `platform.yaml` seed and every L5 route cite that contract. |
| M-16 | L5-00, L5-04 | L5-00 §4.3 `:205`, §13 `:641-653`; L5-04 `:3978, 4284`; L5-06 `:41, 1732-1780`; L5-06 `:712, 2705` | path-ownership | Two ownership traps. (a) The charter routes the §45.4 rebuild runbook to `ops-vm/rebuild/` with no note that the spec names it `docs/control-plane-rebuild.md` and `docs/**` is L0's — every other L5 file handles this explicitly ("If anyone asks for `docs/control-plane-rebuild.md` to be written from this lane: **refuse**"); the charter, which those files cite as authority, is the only place the trap is invisible, and §13 has no row for it. (b) L5-06 asserts twice that `runbooks/credential-rotation.md` is "L0-owned"; PARTITION assigns `runbooks/**` to **no lane** — a sixth silent ownership claim, committed into a tracked YAML. | Add the §45.4 handoff note and a §13 row; change both `runbooks/` strings to `DECLARED-BY-L0` and raise `runbooks/**` as a PARTITION gap. |
| M-17 | L5-07 | `:288-292, 256-276, 60-70` | missing-control | The ASSISTED regime — the mechanism that makes twelve human-gated checks meaningful — is unenforced. `verify-evidence.sh` compares a GitHub **login** (`jq -r .executed_by`) against a git author **email** (`git log -1 --format=%ae`): a bot committing as `actions@github.com` matches no login and passes; a human committing as `login@users.noreply.github.com` always matches and wrongly fails. And `record-evidence.sh` takes `executed_by` from `gh api user`, i.e. whichever identity the running shell holds — which for an AI executor is the same `gh` the shell contract requires it to have. Every ASSISTED SELF-VERIFY is satisfiable without the privileged step happening. | Compare login to login via `gh api …/commits/<sha> --jq .author.login`; require evidence commits signed by an identity in a Lane-5-owned signer list. |
| M-18 | L5-07 | cards at `:93, 1260, 1282, 1495, 2116, 2139, 2310` vs §5.6 Move 3 `:2313-2320` | executability | Every card tells the human to commit under `access/tests/evidence/` on branch `lane/5/07-evidence`; Move 3 then switches to `integration` and reads the evidence from the current checkout. Nothing opens, merges or fetches that branch. Every ASSISTED check stays INCOMPLETE forever even after the human has done the work. | Add an explicit `git fetch origin lane/5/07-evidence && git checkout origin/lane/5/07-evidence -- access/tests/evidence/` step, and a task that opens the evidence PR. |
| M-19 | L5-07 | `:786-813` (T05), `:898-921` (T06), `:1004-1019` (T07), `:1037` (T08) | completeness | Twelve human-action cards are declared under `Writes`, greped by acceptance criteria, and created by no command — the tasks only `mkdir -p access/tests/human-actions` and describe the cards in prose. T05 criterion 1 prints `0` not `9`; T06/T07 criterion 3 error with "No such file or directory". | Add the twelve `cat > … <<'MD'` blocks, using the shape T09/T10/T14 already ship. |
| M-20 | L5-07, L5-04, L5-02 | L5-07 `:148, 514-528, 1355-1392, 1455-1475`; L5-04 `:1440`; L5-02 `:5045, 5000, 5119` | missing-control | Controls that cannot detect what they claim. L5-07's binding rule pairs every negative check with a positive control; three of its own tests ship with none, and NC-08's is wrapped in `if [ -f infra/hermes/background-worker.yaml ]` — a file no lane produces — so it silently skips and reports SUITE OK from negatives alone. L5-04-06's AC #3 states its expected output as a prose comparison ("credential-check line number is **lower** than…"), unmatchable. L5-02-14 tests reproducibility with `git diff --exit-code` on files that are still **untracked** at that point, so both controls pass regardless of content; and X-16 asserts a fourteen-branch property from `origin/integration...HEAD`, one branch. | Add the missing positive controls; replace the prose AC with the SELF-VERIFY's literal token; move the reproducibility check after the commit or use `cmp`; loop X-16 over the fourteen branch names. |
| M-21 | L5-00, L5-01, L5-02 | L5-00 `:238-249, 644-651`; L5-01 `:3090` etc.; L5-02 `:4252`, `:3896` | citation | Identifier-namespace and attribution errors. `C-01..C-12` carries five meanings across the corpus, three of them in the identical range **[R1]**; `E-01..E-11` names L5's *non*-deliverables while L2's `E-1..E-9` names L2's deliverables, separated only by a zero **[R1]**. §99.6 is cited four times (including in shipped Python and committed YAML) for a sentence at §99.5. `check_separation.py` **prints** `QUOTE Section 54.2:` for a sentence §54.2 does not contain (it is §90.3 line 7992, which cites §54.2 parenthetically) and an acceptance criterion asserts the quote is verbatim. | Namespace the ids per lane; correct §99.6→§99.5 in all four places; relabel the printed QUOTE and its criterion. |
| M-22 | L5-00, L5-01, L5-02, L5-04, L5-05, L5-06 | ~20 sites | citation | Invariants cited for propositions they do not contain. Invariant 85 ("GSD Core pinned to a tagged release; third-party Actions to full commit SHAs; reusable workflows by pinned tag") is used ~15 times as the authority for container-image digest pinning, Python `==` pins and IDE-extension pins — and L5-16/17 derive "a tag is not a pin" from it, the opposite of what it permits for two of its three subjects. Invariant 80 ("every control is explicitly classified fail-closed or fail-open") is used six times as authority for defaulting to deny. Invariant 84 ("API keys remain absent from developer environments") is cited as the no-secrets-in-tree rule. Invariant 47 is glossed as "every run writes its result" (that is §53.1). | Cite §36.3/§51.4 for pinning, the specific fail-closed sentence relied on, §40.1/§40.3 for secrets-in-tree, §53.1 for run records. Restrict each invariant to its own text. |
| M-23 | L5-00, L5-02, L5-99 | L5-00 `:653`; L5-02 `:222, 5155`; L5-99 `:681-686` | citation | **Stale escalations against defects the spec has already repaired.** Charter E-10 and L5-02 E-P2-09 both escalate "duplicate AT-110 rows"; there is one AT-110 row (9450) and D113 records the repair. L5-99 certifies the duplicate as "correct" and certifies the signal catalogue as ending at **SIG-46**; the spec has SIG-47, and the anti-hallucination rule at master/07:268 would reject a legitimate SIG-47 reference as fabricated **[R1]**. | Withdraw both AT-110 escalations (keep the §46.6 half, which is real: headings at 4185 and 4189); correct the SIG ceiling to 47 everywhere the closed catalogue is asserted. |
| M-24 | L5-04, L5-03, L5-05 | L5-04 `:3329, 3774, 3818, 2709`; L5-03 `:2438, 2506-2546, 3329`; L5-05 `:1899-1916` | completeness | Twelve-plus configuration keys are read with `require_env`/`${VAR:?}` and defined in no `.example` file, no task and no contract: `DEADMAN_CHECKIN_URL`, `DEADMAN_METRICS_DIR`, `GRAFANA_{SHARED,LAYERB}_{URL,TOKEN}`, `CAPABILITY_HOLDERS_FILE`, `CHANGE_MANIFEST_ID`, `SNAPSHOT_CMD`, `UPGRADE_CMD`, `REVERT_CMD`, `PRODUCT_REGISTRY_FILE`, `RESTORE_RECORD_DIR`, `LAYERB_DB_NAME`, `TEAM_CHANNEL_WEBHOOK`, `SELFVIEW_TEST_SUBJECT_A/B` (the last "exported" by a line that assigns nothing, so the gate runner aborts on its second line every time). L5-03's self-view generator additionally invokes `selfview.sql`, a `layerb_people` pg_service and a PostgreSQL instance that no task creates, with `psql` absent from the tool preflight. | Create the `.example` files, add the keys to the frozen contract where they cross a boundary, and either add a people-store provisioning task or make the self-view refuse cleanly with a routed escalation. |
| M-25 | L5-05, L5-04, L5-03 | L5-05 `:2026-2054`; L5-04 `:1386, 3056`; L5-03 `:1642, 1628` | contradiction | Notification and audit paths that bypass or defeat their own contracts. L5-04-10's `notify-job-result.sh` POSTs straight to a webhook, bypassing the router, channel registry and closed-push-list guard the same lane builds — and job failure is not on §92.11's closed list, while §44.3 routes backup failure to the *product* channel. `ExecStopPost=` fires after every stop, so a `failed` notification is pushed on every **successful** run, and one unit ends in `%i`, valid only in template units, so systemd refuses to load it. L5-03's audit shipper passes three `-k` flags to `ausearch`, which accepts one — only the last key's events are ever shipped off-host, defeating the §90.3 compensating control, with `\|\| true` hiding the shortfall; and its rules file ends `-e 2`, locking auditd immutable so the STOP rule's own remedy ("re-run L5-P3-07") cannot succeed. | Route through the lane's router with the event class from the frozen push list; make `ExecStopPost` conditional on `$EXIT_STATUS` and drop `%i`; loop one `ausearch` per key; state that L5-P3-07 is once-per-boot. |

### MINOR (recorded, not gating)

m-01 charter cross-references §14 where it means §13 (two sites). m-02 §9.3's cited signal-table range excludes SIG-46. m-03 §4.5 quotes a §42.2 heading the spec does not carry. m-04 `l5-validate.sh` has no `cd` to the repo root and no `pipefail`. m-05 L5-01's PT-05 fills an em-dash spec cell and calls it a transcription; its "twelve clauses" and "eight offline" counts are the file's own decomposition presented as spec properties. m-06 L5-02's `at110-not-reexecuted.yaml` fixture tests a different case than its name. m-07 L5-02's `evaluate_envelope.py` emits an undocumented sixth finding class `E0` that L3 will receive. m-08 L5-02's fifth-tier schema is written and never used, and its `required` list omits a field the next task hard-fails on. m-09 L5-03's five `scp -r` deploys are non-idempotent (`/srv/layerb/config` vs `…/config/layer-b`). m-10 L5-04's Prometheus config scrapes one of §41.2's three endpoints while its comment claims three; its `restore_rotation.py` docstring calls a spec "for example" value "literal". m-11 L5-05 attributes `assets.yaml` to §52 (it is a §39.1 code-fence caption) and quotes D80 with added scope. m-12 L5-06's §3 blocker template is wrapped in a single ``` fence containing six nested fences, so it renders broken and cannot be copy-pasted; several table cells carry markdown-escaped `\|` that differs from the same command in the SELF-VERIFY.

---

## 3. VERDICT

# BLOCKED

Lane 5 cannot be dispatched. Twenty-six blocking defects survive verification; five of them stop execution at or before the first task of a file, and three require an L0 decision that no lane agent may make.

**The reasoning, shortest form:**

1. **The lane does not know what its own task list is.** Two complete decompositions (59 tasks in `L5-06`, 93 in the phase files) describe the same subsystems under seven task-id grammars, collide on identical file paths with different content, and reference each other exactly once — through a `grep -qF` on a SELF-VERIFY banner string. Dispatching both produces duplicate skeletons, duplicate systemd units and duplicate Layer B stacks; dispatching one silently drops whichever half the other file owned. (B-01, B-02)

2. **Nothing can run even if the task list were settled.** No branch/merge model exists, so `L5-01` dies at task 3 and `L5-06`'s 59-task chain dies at task 2 (B-03). Five names for the working root, one never assigned, no clone step (B-16). The blocker-issue template — the only escalation route from every STOP rule in two files — fails on a `sed` regex (B-15). `L5-07`'s suite runner hangs forever on its first invocation (B-04), and 13 of its 14 preconditions name paths no lane writes (B-05).

3. **The gates that would catch this are themselves broken.** SELF-VERIFY runs after push in three files (B-17). Eleven of sixteen DoD rows depend on validator modes no task implements, over eight directories no task fills (B-10). Four L5-06 tasks assert counts that fail against their own supplied content (B-07). Six controls fail open, including the one that proves D89's no-machine-bypass rule (M-08). Twenty-three L5-06 gates are satisfied by a tool that only prints its own success string (M-09).

4. **The spec coordinates are wrong everywhere it matters.** Every citation past line ~950 drifts by +2 to +13; three L5-06 `sed` windows print the wrong section entirely; two wrong ranges are frozen into JSON-Schema `const` values across fifteen generated files (B-06). Two escalations are raised against spec defects that D113 already repaired (M-23).

5. **The lane's own review is superseded and wrong.** `L5-99-review.md` — the artifact an L0 reader would open first — reports 76 missing task bodies, two unimplemented subsystems and 76 dangling ids. All 152 bodies exist; K and R are both implemented; the ids resolve. It also certifies the stale AT-110 duplicate and a SIG-46 ceiling. **This document supersedes it.**

**What is genuinely sound and should not be re-litigated:** the subsystem partition is honest (no L5 task writes outside `access/`, `infra/`, `ops-vm/`, `notify/`, `assets/` — re-verified across all 152 bodies); every task carries a SELF-VERIFY block and a STOP rule (152/152); no identifier outside the spec's real ranges is invented; the spec *content* quoted in the lane is overwhelmingly accurate — it is the line numbers, not the sentences, that have drifted.

---

## 4. Shortest ordered fix list to reach READY

Fourteen steps. Steps 1–4 are gates: nothing downstream can be fixed until they land.

| # | Step | Clears | Owner | Kind |
|---|---|---|---|---|
| 1 | **Pick one decomposition of Lane 5** and retire the other; publish a concordance for retired ids; note the `L5-07:1406/1463` string coupling so the renumber does not silently break AT-091. | B-01, B-02, M-12 | **L0** | **Judgment** |
| 2 | **Rule on unassigned subsystems G/H/J/O/P** under one id, recorded in PARTITION, replacing the six conflicting records. Add `runbooks/**` to the ownership table. | B-14, M-16(b) | **L0** | **Judgment** |
| 3 | **Author `contracts/access/access-inputs.yaml`** in Phase 0 with the union of L5-02's and L5-03's required shapes, plus the four ops-VM credential-store values and the two self-view test subjects. Freeze the eleven `event_type` identifiers and the required-status-check context list in the same pass. | B-09, B-18(partial), M-02, M-15, M-24(partial) | **L0** | **Judgment** |
| 4 | **State the branch/merge model** once per file (accumulation branch or predecessor-branching) and rewrite the `git checkout integration` preamble in all downstream tasks. | B-03 | L5 | Mechanical *(after L0 names the model)* |
| 5 | **Re-derive every spec line range** mechanically (heading → next heading − 1) across all nine files; update the two schema `const` values, the "10,214 lines" figure, and the three L5-06 `sed` windows; add the per-task heading assertion. Withdraw the two AT-110 escalations; correct the SIG ceiling to 47. | B-06, B-07(T09 half), M-23 | L5 | **Mechanical sweep** |
| 6 | **One working-root convention** (`CP_ROOT="$(git rev-parse --show-toplevel)"` at the head of every block), a literal clone step, `: "${CP_ROOT:?}"` guards, and removal of the hard-coded `/c/D_Drive/...` path. | B-16 | L5 | **Mechanical sweep** |
| 7 | **Fix the two blocker templates** (`gh repo view --json nameWithOwner`) and add a Phase-0 `gh label create` step for the nine labels. | B-15 | L5 | **Mechanical sweep** |
| 8 | **Reorder every task**: write → SELF-VERIFY + hard foreign-path gate (`exit 1`, not an echo) → `commit`/`push`/`pr`. Applies to all 152 bodies. | B-17, M-13, M-20(partial) | L5 | **Mechanical sweep** |
| 9 | **Windows/Git Bash pass**: `git update-index --chmod=+x` after every `chmod`; `bc`→`awk`; `/tmp` fixtures → repo-relative scratch; pin the `yq` flavour in every preflight; fix the `set -e` negative-test idiom and move the three restores into `trap … EXIT`. | B-08, M-07 | L5 | **Mechanical sweep** |
| 10 | **Fix the ten single-line executable defects**: `lane5.sh` self-glob; `layerb-instance-separate.sh` comment self-match; `layer-a.yaml.tmpl` + envsubst; the four `.timer` templates + a unit-install step + the missing `/var/lib/ops-vm/*` dirs; `luksFormat` isLuks guard; `layerb-admin` exclusion; the resolver exit-code capture; `ausearch` per-key loop. | B-04, B-19…B-21, B-23, B-24, B-25, M-25(partial) | L5 | Mechanical |
| 11 | **Repoint the path-mismatch families**: L5-07's fourteen preconditions to the paths the phase files write; AT-097/AT-089 to `ops-vm/grafana/provisioning`; L5-01's validator glob to L5-01-owned roots. | B-05, B-12, B-22 | L5 | Mechanical *(after step 1 fixes which paths are canonical)* |
| 12 | **Settle `assets/inventory/**` and `notify/**` shape**: one entry schema, one id key, one filename set, one directory-vs-registry decision under PARTITION rule 3. | B-13, M-14 | **L0** | **Judgment** |
| 13 | **Close the publication contract**: add producers for the eleven unproduced `*/published/**` artifacts (or delete them from §7 and the eight empty directories), assign each `l5-validate.sh` mode to the task that authors its data, correct §13 E-01's count, and mark DoD-04/06–15 blocked until their modes exist. Move every calibrated value into the contract or route it to L0. | B-10, B-11, M-11 | L5 + **L0** | Mixed |
| 14 | **Repair the test/evidence layer**: `lane5.sh` rc=2 vs rc=1; coverage `--strict`; revalidation sweep filtered to non-AUTO; the twelve missing human-action cards; login-vs-login evidence comparison plus a signer list; the evidence-branch fetch step; the missing positive controls. Then re-run this review and replace `L5-99-review.md`. | B-26, M-17, M-18, M-19, M-20 | L5 | Mostly mechanical |

**Mechanical vs judgment.** Of the 26 blocking defects, **19 are mechanical** once the four gate decisions land — steps 5–11 and 14 are grep-and-replace sweeps over known line lists, and steps 5, 6, 7, 8 and 9 are five sweeps that between them clear eleven blockers. **Seven require L0**: the decomposition choice (B-01, B-02), the unassigned subsystems (B-14), the frozen contract's shape (B-09, and the four values behind B-18), the inventory/notify shape (B-13), and the publication-contract scope (B-10, B-11). Roughly **70% mechanical, 30% judgment** by defect count — but the judgment 30% gates the mechanical 70%, because steps 4, 11 and 13 cannot be executed until L0 has said which decomposition and which paths are canonical.

**Do not dispatch any part of Lane 5 until steps 1–4 are complete.** Steps 5–9 can then be run as five parallel sweeps by a single agent across all nine files before any task is handed out.

---

*Prepared 2026-09-02. Supersedes `lanes/L5-99-review.md`, which was written against a pre-repair corpus of ~17,000 lines and whose headline findings do not survive re-measurement against the current 32,873.*
