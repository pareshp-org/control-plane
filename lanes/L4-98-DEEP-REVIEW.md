# L4 — Consolidated Lane Review (round 1 + round 2)

**Lane:** L4 — Records, Events, Metrics (subsystems I, N)
**Files in scope:** `L4-00-charter.md`, `L4-01-records-repo.md`, `L4-02-record-schemas.md`, `L4-03-write-paths.md`, `L4-04-attention-ledger.md`, `L4-05-pipeline-and-boards.md`, `L4-06-tasks.md`, `L4-07-tests-and-runbook.md`, `L4-99-review.md`
**Spec of record:** `C:/D_Drive/PS/MultiProduct/Research/MultiProduct_MasterSpec_v4.0.md` — **10,231 lines** (verified with `wc -l`)

**Raw findings consolidated:** 257 (round 1: 92, across L4-00 / L4-01 / L4-05 / L4-99; round 2: 165, across L4-02 / L4-03 / L4-04 / L4-06 / L4-07).
**Raw blockers:** 74. **After deduplication: 28 blocking clusters + 12 file-local blockers = 40 blocking entries.**

---

## 1. Deduplication summary

Twelve cross-file root causes account for 46 of the 74 raw blockers. They are merged into single clusters below and carry a `C-nn` id.

| Cluster | Root cause | Raw findings merged | Files affected |
|---|---|---|---|
| C1 | Written against a 10,214-line spec revision; the spec is 10,231 | 31 | all 9 |
| C2 | SIG-43 "double assignment" is a phantom; SIG-47 already exists | 4 | L4-00, L4-06, L4-07, L4-99 |
| C3 | `gh api --jq -r` / `--jq -S` — invalid flag binding | 2 (73 call sites) | L4-01, L4-06 |
| C4 | `gh pr create/view --repo-dir` and two broken repo-slug `sed`s | 4 | L4-00, L4-02, L4-03, L4-04 |
| C5 | Store-marker count is 17 / 18 / 19 in three documents | 6 | L4-00, L4-01, L4-03, L4-06, L4-07 |
| C6 | Two incompatible decompositions of L4 Phases 2–7 | 8 | L4-02, L4-03, L4-04, L4-05, L4-06, L4-99 |
| C7 | `D-L4-01/02/03` name six different decisions | 2 | L4-00, L4-01 |
| C8 | Two binding workspace conventions, two variable-name sets | 4 | L4-00, L4-01, L4-02, L4-03, L4-06, L4-07 |
| C9 | Two mechanisms create `control-plane-records` | 2 | L4-00, L4-01 |
| C10 | `blocker` / `lane-4` / `assisted` labels never created | 4 | L4-01, L4-05, L4-06, L4-07 |
| C11 | One branch per phase vs PARTITION.md line 34 (one per task) | 5 | L4-00, L4-02, L4-03, L4-07 |
| C12 | `.l4-venv` / `$L4_PY` vs bare `python3` | 2 | L4-02, L4-05 vs L4-04, L4-07 |

The remaining 28 raw blockers are file-local and are listed individually.

---

## 2. Spot-check of the most serious claims

Every check below was re-run against the live files and the live spec. **All survived. None refuted.** One claim (#16) required narrowing.

| # | Claim | Command run | Result |
|---|---|---|---|
| 1 | PyYAML resolves unquoted timestamps to `datetime`, so every "valid" fixture is rejected by a `"type":"string"` schema | `yaml.safe_load('timestamp: 2026-09-14T02:11:00Z')` under PyYAML 6.0.2 / jsonschema 4.23.0 | **CONFIRMED.** Returns `datetime.datetime(2026,9,14,2,11,tzinfo=utc)`; `Draft202012Validator` reports `... is not of type 'string'`. L4-02 line 424's fixture is unquoted; `record.base.schema.json` `$defs.timestamp_utc` is `"type": "string"` with an ISO `pattern`. |
| 2 | L4-02's SELF-VERIFY `sed -n '8952p' … awk -F'·' '{print NF}'` expecting `86` fails | `sed -n '8952p'` and `sed -n '8963p'` on the spec | **CONFIRMED.** 8952 = `product: <product>` → `NF=1`. The 86-entry taxonomy sentence is at **8963** → `NF=86`. |
| 3 | `gh api … --jq -r .private` aborts | `gh api "/repos/cli/cli" --jq -r .private` (gh 2.81.0) | **CONFIRMED.** `accepts 1 arg(s), received 2`. 18 sites in L4-06, 55 in L4-01. |
| 4 | Three-way store-count contradiction | `grep` across the three files | **CONFIRMED.** L4-01:355 requires `19`; L4-03:98/168/193 requires `18`; L4-00:544 tests `18` and prints `STORES OK 17/17`. §97.2 has 17 store rows (8857–8873) + `events/` = 18; L4-01 adds a 19th (`bootstrap`). |
| 5 | The SIG-43 double assignment is a phantom | `grep -n '^\| SIG-4[0-9]'` | **CONFIRMED.** SIG-43 = *Founder operating load* (4576), appearing once. *Unclosed learning-loop items* = **SIG-47** (4580). The renumbering is recorded at D113 (10227). L4-06's D-L4-TL-4 and L4-07's D-L4-P7-02 both raise a decision for a conflict that does not exist. |
| 6 | Writer CLI mismatch between L4-03 and L4-07 | `grep '# Usage:'` L4-03 vs `grep -- '--store\|--from-file'` L4-07 | **CONFIRMED.** L4-03:902/910/1004 define `record-write <store> <id> <body-file>`, `event-append <event_type> <actor> <product> <subject_ref>`, `record-verification-result <product> <item> <mechanism> <pass\|fail> <evidence_link> <actor>` — all positional. L4-07 calls all three with long options that exist nowhere. |
| 7 | `RECORDS_ROOT` is read by nobody | `grep -l RECORDS_ROOT lanes/L4-*.md` | **CONFIRMED.** Only L4-07. The writers read `CPR_ROOT` (67 occurrences in L4-03). The sandbox guarantee is inert. |
| 8 | L4-03's board-event ids are absent from the L4-02 taxonomy | `grep 'event_type'` L4-03:1240-1285 vs L4-02:524-600 | **CONFIRMED.** L4-03 emits `moved_to_ready`, `assigned`, `re_plan_triggered`; the taxonomy ids are `work_item_moved_to_ready` (n:2), `work_item_assigned` (n:3), `replan_triggered` (n:12). |
| 9 | L4-03 T315's human-record guard always passes | `printf 'a\nb\n' \| python - arg <<'PY' … PY` | **CONFIRMED.** `sys.stdin.read()` returns `''`; the piped diff is discarded by the heredoc. |
| 10 | L4-04's `INPUTS=9` is wrong | `grep -o 'input/sc-[0-9a-z-]*\.yaml' \| sort -u` | **CONFIRMED.** Eleven files match `sc-*.yaml` (sc-1..sc-8 plus sc-9-people / -calendar / -absence). |
| 11 | L4-07's AT-046 minimum baseline set is fabricated | `grep -n 'minimum set\|minimum-set'` spec; `sed -n '9984,9990p'` | **CONFIRMED.** Zero hits. §103.14 is one paragraph about pre-Phase-1 banded self-estimates and names none of the five metric ids. |
| 12 | L4-07's `grep -c … \|\| echo 0` double-prints on the happy path | reproduced in Git Bash | **CONFIRMED.** Prints `ABSENT=0` then a bare `0`. |
| 13 | Both repo-slug `sed`s are broken | ran both expressions against HTTPS and SSH remotes | **CONFIRMED.** L4-02's yields `acme/control-plane.git` for both forms; L4-04's leaves the SSH form entirely unsubstituted. |
| 14 | The citation drift is systematic, not isolated | `grep -n '^\| D88 \|^\| AT-105 '`, invariant rows, §97/§99/§101/§103 headings | **CONFIRMED.** D65=10149, D77=10171, D88=10192, D102=10206, D107=10216, D111=10220; AT-044=9377, AT-046=9379, AT-105=9386; invariants 15=9482, 46=9525, 47=9526, 48=9527; §97.1=8846, §97.2=8853, §97.3=8938, §97.4=8965, §97.5=8993, §99.2=9195, §101.7=9517, §103.14=9984. Every "actual" value asserted by the reviewers matches. |
| 15 | L4-02 T201's preflight validates the tree, then destroys it | read L4-02:220-232 | **CONFIRMED.** The four `test -d` guards run *before* `checkout -B … origin/integration`, and the following `mkdir -p` does not recreate `schemas/records` or `metrics/taxonomy`. |
| 16 | `lane-selfcheck.sh --coverage` and `--stores` are unbuilt | `sed -n '540,550p' L4-00` vs `grep 'lane-selfcheck.sh --' L4-06` | **CONFIRMED, NARROWED.** The charter script implements `--paths` and `--stores` only, with `exit 2` on anything else, so L4-06 row 6's `--coverage` is genuinely unbuilt. The stronger claim that `--stores` is also unbuilt is **narrowed** — it exists, but is hard-coded to `18`, which is cluster C5. |

---

## 3. Defect table — BLOCKING

Ranked by severity, then by how much execution each one stops.

| # | Sev | File(s) | Location | Category | Problem | Fix |
|---|---|---|---|---|---|---|
| B1 | blocker | all 9 | header line 7/8 of each; ~200 inline citations; `store-map.yaml`, `sources.yaml`, `calibration.yaml`, `at-map.yaml`, `derived-metrics.yaml`, `rqm-contract.yaml`, `CONTRIBUTING.md`, 20+ JSON `description` fields | citation | **C1.** Every file declares the spec as 10,214 lines; it is 10,231. Every §29 / §40 / §52 / §53 / §57 / §58 / §67 / §97 / §99 / §101 / §103 / D-id / AT-id / SIG-id / invariant anchor is stale by +2 to +12, and most now land on real but unrelated text. Six anchors are *executable* and halt their phase: L4-02 T203 (`8952p` expects `SPEC=86`, gets 1); L4-05 P5-16 crit 3 (13 of 14 lines lack `DevLake`); L4-05 P5-22 crit 6 (`9513p` grep → 0); L4-05 P5-23 crit 3 (`4459p` grep → 0); L4-03 T314 (`grep -c '8841'` and `'3667'` must each return 1); L4-00 T006 (`$3+0 > 10214` ceiling). The wrong numbers also ship permanently into control-plane and control-plane-records artifacts. | One mechanical re-derivation pass against the 10,231-line spec across all nine files and every heredoc. Correct the `10214` constant at L4-00:8/666/676, L4-05:9, L4-07:7, L4-04:8, L4-99:5. Then convert bare line numbers into grep assertions (`sed -n '<N>p' "$SPEC" \| grep -q '<literal>'`) so the next amendment fails loudly instead of silently. |
| B2 | blocker | L4-02, L4-03, L4-04, L4-05, L4-06, L4-99 | L4-06 §2 rows 20–117 vs the task bodies in L4-02/03/04/05 | contradiction / path-ownership | **C6.** Phases 2–7 are decomposed twice, into incompatible layouts, by documents an executor may be handed together. `record.base.schema.json` (L4-02 T202) vs `record.envelope.schema.json` + `defs/timestamp.schema.json` (L4-06 P2-03/04). `validate-schemas.sh` with `--coverage\|--lint\|--fields\|--time\|--all` (L4-02 T223) vs `--selftest\|--only` plus `requirements.txt` and `lib/schemaload.py` (L4-06 P2-02). `store-map.yaml` vs `store-inventory.yaml`. `metrics/attention/**` flat with 15 files (L4-06 P6-01..15) vs a `lib/` layout (L4-04 T402..T414) — and `metrics/attention/sources.yaml` is claimed outright by both. The RQM detector is claimed three ways: `tools/records/rqm-detect` (L4-04 T417), `tools/records/rqm/*.py` ×9 (L4-06 P5-05..13), and L4-05:3609 which tells the executor **"Do not create it here."** `record-write` / `event-append` / `record-verification-result` are shell wrappers in L4-03 and Python modules in L4-06 P4-03/05/06. L4-05's universal precondition E2 needs `tools/records/check.sh` from L4-P2-01, which no L4-02 task writes. Phase numbering collides too (L4-06 Phase 3 = `metrics/**`; L4-03 Phase 3 = write paths). | **L0 decision before dispatch.** Pick one decomposition; delete or explicitly mark non-executable the loser's rows — a concordance is not enough, because two agents holding two documents will both write `tools/records/record-verification-result`. Then re-point L4-05's E2/E4/E7 and L4-04/L4-07's cross-references at the surviving names. |
| B3 | blocker | L4-01, L4-06 | L4-01: 55 sites (45, 170–1517) incl. `--jq -S` at 1474; L4-06: 18 sites (254–269, 593–607) | command-error | **C3.** `gh api` declares `-q, --jq string`, so `--jq -r .private` binds the jq program to the literal `-r` and passes `.private` as a second positional. **Verified against gh 2.81.0: `accepts 1 arg(s), received 2`.** Every SELF-VERIFY in L4-01 T01/T02/T03/T04/T08/T10/T11/T12 and its phase exit gate contains at least one; L4-06 halts at task 2 of 117. | Mechanical sweep: delete the stray `-r` / `-c` / `-S`. `gh api --jq` already emits raw scalars. |
| B4 | blocker | L4-00, L4-01, L4-03, L4-06, L4-07 | L4-00:544; L4-01:355/370/390/1523; L4-03:98/154/168/181/193/209; L4-06:565; L4-07:69/218/631/684 | contradiction | **C5.** Three values for one count. L4-01 creates and demands **19** `.gitkeep` markers (17 §97.2 stores + `events` + `bootstrap`). L4-03 entry condition E1 and `STORES=18` demand **18**, and its STOP rule forbids the agent creating or removing anything — so Phase 3 never starts against a correctly executed Phase 1. The charter's `lane-selfcheck.sh --stores` tests `18` and prints **`STORES OK 17/17`**, so DoD-1 fails permanently once L4-01 has run. L4-07 says "18 stores" in four places and "17/17" in three. | L0 fixes the inventory (17 §97.2 rows + `events/` = 18) and rules on whether `bootstrap/` is a tracked nineteenth. Then set one literal in all five files, and make the charter's message agree with its own test. |
| B5 | blocker | L4-02 | T202 line 424 (`record-base-probe.yaml`); every valid fixture in T204 and T206–T222 | executability | `yaml.safe_load` resolves unquoted ISO timestamps to `datetime.datetime` and unquoted dates to `datetime.date`; `record.base.schema.json` declares `timestamp_utc` / `date_utc` as `"type": "string"`. **Reproduced under the pinned PyYAML 6.0.2 + jsonschema 4.23.0.** Every "valid" fixture is rejected: `POS=1`, T202's `PROBE=0` fails, and the phase cannot begin. `timestamp:` appears on all 17 valid fixtures plus the probe plus `event-plan-approved.yaml`; the date fields add more. | Load with a `SafeLoader` subclass whose `tag:yaml.org,2002:timestamp` implicit resolver is removed, in `lib/validate.py`, `validate-no-names.sh` and `test-negative.sh`. Do **not** let an executor "fix" it by quoting fixtures — real writers emit unquoted YAML. |
| B6 | blocker | L4-03, L4-07 | L4-03:902/910/1004 (positional) vs L4-07:663-671, 841, 849, 942-954, 1573-1575, 1765-1769 | contradiction | **Verified.** L4-07 calls `record-write --store … --validate-only --from-file … --fixture-mode --check-sla`, `event-append --event-type … --from-file`, `record-verification-result --product … --result …`. None of those flags exists in L4-03. Under `set -euo pipefail`, `record-write` receives `$1=--store` and either aborts or writes a store literally named `--store`. Corpus B (T04), suite 2 (T06), suite 3 (T07), `test-rvr.sh` (T11) and the AT-105 half of suite-at (T12) cannot run — and `assert_rejects` reports PASS on the usage error, so **five of nine suites are vacuously green**. | L0 freezes one writer CLI in `contracts/tools/records/CLI.md`; both documents are reissued to it. |
| B7 | blocker | L4-07 (vs L4-03) | L4-07:658, 1564, 1579 | executability | **Verified: `RECORDS_ROOT` appears in no other L4 file.** The writers read `CPR_ROOT`. The whole sandbox guarantee of Phase 7 is inert: `generate-corpus.sh`, `test-rvr.sh` and `test-read-across.sh` write into the **real** `control-plane-records` tree, breaching §4.1 R6 and §4.7 F4, and then trip the phase's own `RECDIRTY` STOP rule. `test-read-across.sh --unreachable` also becomes a false negative against a correct implementation. | Rename to `CPR_ROOT` throughout L4-07 (or L0 names one variable and both files are amended together). Add an assertion that the sandbox actually received the writes. |
| B8 | blocker | L4-03 (vs L4-02) | L4-03 T306:1043-1055, T312:1789-1795 vs L4-02 `uat.schema.json`:1558, `deployment.schema.json`, `restore-test.schema.json` | contradiction | The records the writers emit are rejected by the schemas the same lane publishes. The RVR handler writes `mechanism, item, evidence_link, reported_by, write_path` into a UAT record; `uat.schema.json` sets `additionalProperties: false` and requires `executed_by, uat_contract, environment`. `deploy-emit` writes `write_path` and `deployed_by`, neither declared in `deployment.schema.json`. T307's `armed-pass` case and T313's case 1 fail. Whether T312/T313 pass at all flips on whether `jsonschema` is importable — T301's E4 proves only `import yaml`. | Reconcile the record envelopes with the emitters, or have L4-T306 build bodies from the target schema's `required` list as T312 already does. Add `python3 -c "import jsonschema"` as entry condition E6 with its own SELF-VERIFY line. |
| B9 | blocker | L4-03 (vs L4-02) | L4-03 T308:1247-1279; T310 expects `TAXONOMY-CONFORMANCE OK 9/9` | contradiction | **Verified.** `board-events.yaml` declares `moved_to_ready`, `re_plan_triggered` and `assigned`; the L4-02 taxonomy ids are `work_item_moved_to_ready` (n:2), `replan_triggered` (n:12), `work_item_assigned` (n:3). The first two fail the token regex and Phase 3 stalls permanently at T310. The third passes **spuriously** — the taxonomy row's free-text `entry: "assigned"` matches the grep — so `board-emit assigned` emits an `event_type` outside the closed enum that spec line 8959 says CI rejects, while the SELF-VERIFY reports 9/9. | Rename the three ids to the L4-02 taxonomy, and rewrite `validate-board-events.sh` to `yaml.safe_load` the taxonomy and compare only against `id:` values, never a free-text grep. |
| B10 | blocker | L4-06 | §0.6:106-120, §4.1:544-556 | missing-control | For 98 of 117 tasks the *only* self-verification is `check.sh <TASK-ID>`, which runs `tools/records/checks/<TASK-ID>.sh` — a file the same task is told to author, with no constraint on its contents. `#!/usr/bin/env bash\nexit 0` produces the two expected lines. §0.8 rule 9 ("a check that cannot fail is not a check") has no enforcement over the check files themselves. | Require each check file to invoke the task's own §2 acceptance command and compare stdout to a literal, plus a paired negative fixture proving it fails when the artifact is absent. Add a `check.sh --audit` mode run by each phase exit gate. |
| B11 | blocker | L4-06 | §2 rows 49-51, 56-65, 66-68, 70-77, 79-93, 94-95 | executability | Six acceptance harnesses are invoked by 33 tasks that run *before* the task that creates them: `validate-taxonomy.sh` (created P3-03, used by P3-01/02), `validate-boards.sh` (P5-03, used by P5-01/02), `test-writers.sh` (P4-09, used by P4-01..05/07/10), `test-rqm-detector.sh` (P5-12, used by P5-05..11), `test-derivation.sh` (P6-15, used by P6-01..14), `validate-sources.sh` (P7-02, used by P7-01). The file says "Work it top to bottom. Do not reorder." All 33 exit 127. | Move each harness into a skeleton task before its first consumer (as P2-02 already does), and list the harness path in the Files-touched column of every task that adds a subcommand. |
| B12 | blocker | L4-06 | §0.1:21-32; §2 rows 12/13/16/17/18; §4.3:603-609 | executability | `${BRANCH_RS_ID}`, `${TAG_RS_ID}`, `${APP_SLUG}` and `${RW_TOKEN}` appear in acceptance and SELF-VERIFY commands and are defined nowhere. §0.1 is the only export block and defines none. An executor cannot obtain a ruleset id, an app slug or an installation token from the text. | Add capture commands to §0.1, or have P1-T05/T06/T09/T10 each write its id into `${SECRETS_DIR}` and have the SELF-VERIFY read the file. |
| B13 | blocker | L4-06 | §0.3:60-66, §0.4:84-90; every control-plane task from L4-T004 | executability | `$CP_DIR` and `$RECORDS_DIR` are exported but no task ever clones either repository; §0.1 only runs `mkdir -p "$WORK"`. The first control-plane step, `cd "$CP_DIR" && git fetch origin`, fails on L4-T004. | Add explicit clone-or-STOP steps to §0.1 for `${CP_SLUG}` and, after P1-T02, for `${RECORDS_SLUG}`. |
| B14 | blocker | L4-06, L4-00 | L4-06 §0.3:68 vs §2 rows 4-5; L4-06 §2 row 6 vs L4-00:540-550 | executability / completeness | §0.3 runs `lane-selfcheck.sh --paths` after **every** control-plane task, "no exceptions" — but the script is created by L4-T005, so L4-T004 and L4-T005 themselves exit 127 and STOP before a single file is committed. Separately **(verified)**, the charter script implements `--paths` and `--stores` only, with `exit 2` on anything else; L4-06 row 6's acceptance command is `--coverage`, which no task builds. | Exempt L4-T004/T005 from the selfcheck line; and either add `--coverage` to the charter script or change row 6's acceptance command. |
| B15 | blocker | L4-06 | §0.5:98-104 vs §0.6:110-120 | contradiction | §0.5 defines a three-state contract (`OK`/0, `FAIL`/1, `ERROR`/3, "Exit 3 is never a pass") and the dispatcher collapses it: `if bash "$F" >/dev/null 2>&1; then PASS; else FAIL; fi`. Every exit 3 (fail-closed, unreachable store, missing input) reads as `FAIL`, and its `ERROR <reason>` text is discarded by the redirection. The fail-closed distinction the lane is built on is invisible in the one command every task runs. | Replace the dispatcher body with a `case $rc in 0\|1\|3\|*)` and propagate `$rc`; update §4.1's expected-output block. |
| B16 | blocker | L4-06 | §5, P4 exit gate:624 | command-error | `a && b && c \|\| true` — if `test-writers.sh` or `test-rvr.sh` fails, the chain short-circuits, `true` runs, and the gate **exits 0** having printed no `FAIL-CLOSED` line. An agent checking the exit code records a pass on a broken Phase 4. This is the exact silent-pass failure §0.5 exists to prevent. | Split into two statements: assert the first two with an explicit failure branch, then invert the read-across check. |
| B17 | blocker | L4-06 | §2 rows 49 and 50 (L4-P3-01, L4-P3-02) | judgment-required | The task requires minting ~95 permanent public `event_type` identifiers from a prose list, against a spec that says an identifier is "never renamed once shipped" (8959); P3-02 requires inventing every per-type payload schema. PARTITION.md line 47: "No task may require designing, choosing, or interpreting." Two parallel developers produce two different enums. | L0 publishes the frozen `event_type` list and payload field sets under `contracts/**` in Phase 0; P3-01/02 become transcription-and-verify tasks. |
| B18 | blocker | L4-03 | T315:2156 | command-error | **Verified.** `printf '%s\n' "$DIFF" \| python3 - "$W" <<'PY' … PY` — the heredoc wins on fd 0, `python3 -` consumes it as the program, `sys.stdin.read()` returns `''`, and the diff is discarded. The guard always reports `HUMAN-RECORD OK 0 files` for any diff. This is the only mechanical enforcement of the no-hand-edit rule anywhere in the estate; the file says so itself at line 2247. | Move the checker to `tools/records/lib/human_record_check.py` and pipe into it, or pass the diff as a file argument. |
| B19 | blocker | L4-03 | 557-560, 1171-1174, 1921-1924, 2316-2325 | executability | Four command blocks export only `CP_ROOT`, then invoke scripts opening with `: "${CPR_ROOT:?CPR_ROOT not set}"` — `validate-write-paths.sh`, `test-rvr.sh`, `test-deploy-emit.sh` and `test-write-paths.sh` all abort *after* the artifact has been committed. §2 explicitly instructs "Re-export these at the start of every task" and these four do not. Worse, line 2325 runs `git -C "$CPR_ROOT" push -u origin main` with `CPR_ROOT` unset, expanding to `git -C  push …`. | Add the `CPR_ROOT` export to the four blocks; audit T304/T308/T311 the same way. |
| B20 | blocker | L4-03 | 2325; PARTITION.md 32-34 | contradiction / path-ownership | `git -C "$CPR_ROOT" push -u origin main` pushes T314's `CONTRIBUTING.md` commit straight onto `main` of `control-plane-records`. No task in the file branches, fetches, rebases or PRs the records repository; T316's PR is for control-plane only. `emit_commit` (line 877) likewise commits to whatever branch `$CPR_ROOT` happens to be on, so a stray self-test failure lands on `main` too. | Add `fetch --all --prune` plus `checkout -B lane/4/03-write-paths origin/main` for `$CPR_ROOT` to T301, a `CPRBRANCH=` SELF-VERIFY line, and a second PR against the records repository. |
| B21 | blocker | L4-00, L4-03 | L4-00:718/721/730-731; L4-03:2326/2329/2340 | command-error | **C4.** `gh pr create --repo-dir` / `gh pr view --repo-dir` — no such flag; the selector is `-R/--repo OWNER/REPO`. Six invocations abort with `unknown flag`, so neither phase opens its PR. L4-03's own SELF-VERIFY at 2352-2353 uses the correct form, contradicting its task body. | Replace with `--repo "$REPO_SLUG"` (see B22 for the slug). |
| B22 | blocker | L4-02, L4-04 | L4-02:3742/3767; L4-04:3524/3541 | command-error | **C4, verified by running both expressions.** L4-02's `sed -E 's#.*[:/]([^/]+/[^/]+)(\.git)?$#\1#'` returns `acme/control-plane.git` for HTTPS **and** SSH (ERE leftmost-longest swallows `.git`); L4-04's `sed 's#.*/\([^/]*/[^/]*\)\.git#\1#'` leaves the SSH form entirely unsubstituted. Both feed `gh pr create --repo`, which fails "Could not resolve to a Repository". L4-02:3767 and L4-04:3541 additionally call `gh pr view` with no repository context at all, in files that state the working directory is reset between calls. Neither file establishes `gh` or `gh auth status` as a precondition. | Strip `.git` first, then extract: `sed -E 's#(\.git)?$##; s#.*[:/]([^/]+/[^/]+)$#\1#'`. Hoist into `$REPO_SLUG` shared by create and view. Add a STOP rule if the slug does not match `^[A-Za-z0-9._-]+/[A-Za-z0-9._-]+$`, and add `gh --version` / `gh auth status` preconditions. |
| B23 | blocker | L4-01, L4-05, L4-06, L4-07 | L4-01 §0.4:79; L4-05:2150/2578/2985; L4-06 §0.7:143-160; L4-07 §0.4:106-108 | executability | **C10.** Every STOP rule in the lane routes through `gh issue create … --label blocker --label lane-4` (and L4-05's `--label assisted`). None of the three labels is created by any task in any L4 or L0 document; `gh issue create --label X` hard-fails with `could not add label: 'blocker' not found` and creates no issue. The escape hatch is unreachable at exactly the moment it is needed. In L4-06 the failure compounds: §0.1's first STOP fires while `ORG` is still the placeholder, at which point `CP_SLUG` is malformed and `gh` cannot run at all. | Add a Phase-0 task running `gh label create blocker / lane-4 / assisted --repo "${CP_SLUG}" \|\| true`, and give the blocker template an ORG-independent fallback (write the YAML to `${WORK}/BLOCKER-<TASK-ID>.yaml` and print the path). |
| B24 | blocker | L4-01 | §0.4:74-110 (fence opens 74, inner fences 88–100); T12 block opens 1338, closed by the inner fence at 1371 | executability | Bare ``` fences inside `<<'EOF'` heredocs terminate the enclosing markdown code block. An agent copying §0.4 gets `gh issue create … --body "$(cat <<'EOF'` with the heredoc never terminated, and the shell hangs. The T12 block is truncated mid-heredoc; the remaining 77 lines — including `git push` and `gh pr create`, T12's entire deliverable — fall outside the block. | Re-fence with four-backtick or `~~~~` outer fences, or move the heredoc bodies into separate files. |
| B25 | blocker | L4-00, L4-01 | L4-00 §9:209/216/223 and T007:720 vs L4-01:118/128/141; L4-02:44-47 | task-id / contradiction | **C7.** `D-L4-01/02/03` name six different decisions across two files in the same lane, and L4-02 continues the charter's numbering at D-L4-04..07. L4-01 §0.1 STOPs if D-L4-01 is unanswered and T08 STOPs on D-L4-02 — neither can be resolved, because L0 does not know which question is meant. | L0 renumbers one side (L4-01's three become D-L4-08/09/10) and updates every referring STOP rule and PR body. |
| B26 | blocker | L4-00, L4-01 | L4-00 T002:318-334 vs L4-01 P1-T02:236-246 and P1-T03:302-350 | contradiction | **C9.** Two documents in one lane stand up `control-plane-records` incompatibly: the charter does `mkdir -p "$CPR_ROOT"; git init -b main` at `C:/D_Drive/…/repos/control-plane-records` with no remote and no push; L4-01 does `gh repo create "${ORG}/control-plane-records" --private --add-readme` and clones into `$HOME/l4work/`. Running both yields two divergent repositories, or `gh repo create` failing "Name already exists", or an orphan local repo whose commits never reach GitHub. | L0 picks one creation path and deletes the other task. |
| B27 | blocker | L4-00, L4-01, L4-02, L4-03, L4-06, L4-07 | L4-00 §10:238-241; L4-01 §0.1:20-33; L4-06 §0.1:21-32; L4-07 §0.1:33-41 | contradiction | **C8.** Two "binding" workspace conventions with two variable-name sets: `CP_ROOT` / `CPR_ROOT` at `C:/D_Drive/PS/MultiProduct/Code/repos/**` (charter, L4-02, L4-03) versus `CP_DIR` / `RECORDS_DIR` at `$HOME/l4work/**` (L4-01, L4-06, L4-07). No command from either side runs under the other's exports, and the charter's L4-T001 hard-STOPs on exactly the state L4-06 §0.1 produces. L4-07 lines 40-41 paper over it with `export CP_ROOT="$CP_DIR"` and the comment "Same directories" — **which is false**. | L0 fixes one root and one variable-name set; all six documents are reissued to it. Delete the false comment immediately. |
| B28 | blocker | L4-00, L4-06, L4-07, L4-99 | L4-00:186; L4-06 §1 D-L4-TL-4:207-214 and §3:449, 522; L4-07 §1 D-L4-P7-02:185-199 and 1616, 1802 | citation | **C2, verified.** SIG-43 is assigned once (*Founder operating load*, spec 4576); the learning-loop signal is **SIG-47** (4580), renumbered by D113 (10227). The claimed collision does not exist. The consequences ship: L4-06 emits `signal_id: PENDING-D-L4-TL-4` into `metrics/signals/sig-source-map.yaml` and blocks P3-06 and P7-14; L4-07 ships a permanent `SKIP SV-SIG43 DEFERRED-D-L4-P7-02`; L4-00 §8 wires the postmortem/incident stores to the wrong signal id; L4-99 certifies the phantom as personally verified. L4-06's five anchor lines (4535/4546/4570/4571/4575) also resolve to SIG-02/13/37/38/42 — the correct values are 4539/4550/4574/4575/4579. | Delete D-L4-TL-4 and D-L4-P7-02. Unblock P3-06 and P7-14. Set `signal_id: SIG-47` with source `records/postmortems/` **plus** `records/incidents/` — so the map's source field must become a list — anchored at 4580. Replace L4-07's skip with a real assertion and raise its suite counts from 16/16 to 17/17. |

### File-local blockers, folded into the same fix passes

| # | Sev | File | Location | Category | Problem | Fix |
|---|---|---|---|---|---|---|
| B29 | blocker | L4-04 | 3518 | command-error | `sh lane-selfcheck.sh` with no argument hits the charter's usage default (`exit 2`); there is no `set -e`, no acceptance criterion and no SELF-VERIFY line covering it. Charter DoD-17 ("No L4 file appears outside the owned paths") is silently skipped at the one moment the branch is pushed. | Pass `--paths`, add a SELF-VERIFY row with the charter's literal PATHS-OK line, and a STOP rule on non-zero exit. |
| B30 | blocker | L4-04 | T412 acceptance criterion 1 and SELF-VERIFY (`INPUTS=9`) | command-error | **Verified: the glob matches 11.** "Expected output, exactly" cannot be met; the agent stalls or files a spurious blocker even though all nine cases pass. | Narrow the glob to `sc-[1-9].yaml` (→ 9), or change the literal to 11. |
| B31 | blocker | L4-07 | T01:305-341, STOP rule 374 | completeness | The entry gate STOPs the phase unless 26 artifacts are present, but `tools/records/read-across`, `validate-freshness.sh`, `validate-retention.sh`, `metrics/freshness/write-freshness.yaml` and `metrics/register/validate-sources.sh` are built by **no task in any L4 document** — they appear only as DoD proof commands in the charter. The gate simultaneously omits `rqm-detect`, which T10 invokes. Phase 7 can never start; loosened, it then fails on a binary the gate never named. | L0 assigns build tasks for the five orphan artifacts; add `rqm-detect` and `rqm-emit` to the inventory and change `ENTRIES=26` to 28. |
| B32 | blocker | L4-07 | 1613-1615, cited from 1554 | citation | Three of four SIG-source assertions contradict the spec column they cite: SIG-06's Source is "Planning board" (4539), SIG-17's is "Control plane" (4550), SIG-41's is "Support-intake records" (4574); the file asserts `events/`, `records/restore-tests/` and `records/support/`. Either the suite fails against a spec-faithful map, or L4 is forced to write a map that contradicts the spec — and the STOP rule forbids the executor resolving it. | L0 decides whether `sig-source-map.yaml` records the spec's Source column verbatim or a records-store path, publishes it as a Phase-0 contract, and both sides are reissued. |
| B33 | blocker | L4-07 | 1681-1685, 1748-1757; STOP rule 1830 | citation | **Verified fabricated.** The "AT-046 minimum baseline set" is presented as a verbatim §103.14 quotation and hard-codes five metric ids. `grep -n 'minimum set\|minimum-set'` over the spec returns **zero hits**; §103.14 (9984-9986) is one paragraph about banded self-estimates and names none of them. Four of the five metric ids are invented here. The assertion and its STOP rule rest on a contract that does not exist. | Delete the quotation and the invented `want` list. If a minimum set is wanted, L0 defines it as a Phase-0 contract naming exact metric ids. Otherwise reduce AT-046 to its two real assertions and change 12/12 to 11/11. |
| B34 | blocker | L4-07 | 1675, 1737-1742, 1786-1794, 1802 | contradiction | AT-044 carries three expected values in one task (3.75 in the table, 3.75 written into the file, 2.75 after an in-line `sed` "correction", 2.75 expected). The second `sed` at 1791 replaces a byte-identical string — a no-op presented as a repair. The assertion does `format(json.load(sys.stdin)["hours"], ".2f")` while every DC expected file defines `hours` as a nested dict → `TypeError`, so AT-044 can only ever error. The prose says "DC-1…DC-5" while the command passes only DC-1/3/4/5. | Delete the correction block, write `2.75` directly, fix the prose to name the four DCs, and freeze the `--query-product` output shape as a Phase-0 contract with L4-04. |
| B35 | blocker | L4-07 | defined at 129 and 386; used at ~40 sites across T03–T13 | executability | `l4_guard` is defined only inside T02's command block, yet every creating command in T03–T13 depends on it — and line 2062 of the same file says shell state does not persist between calls. `l4_guard: command not found` → 127 → the `&&` short-circuits → the heredoc is **silently skipped and no file is created**. The same applies to `$CP_DIR`, `$L4_TMP`, `$L4_BRANCH`, `$FX`, `$NEG`, `$D88`, `$BM`, `$RQ`, `$AI`, `$AE`. | Prefix every task block T02–T14 with the §0.1 exports and the literal `l4_guard` definition, or write the guard to `tools/records/test/guard.sh` in T02 and source it. |
| B36 | blocker | L4-00, L4-04 | L4-00 T001:260; L4-04 T401 | command-error | `git checkout -B <shared branch> origin/integration` force-resets the branch. All seven charter tasks live on one branch and T004 expects T002–T006 work present; re-running T001 — the documented recovery from a STOP — silently destroys committed T004/T005/T006 work, with no acceptance criterion that would notice. There is no `set -e` and no gate on the `git status --porcelain` at 259. L4-04 T401 has the identical defect for T402 onward, and neither file verifies that `origin/integration` exists at all. | Use `git switch -c` (which refuses if the branch exists) plus a documented resume path; gate on the porcelain output; add `rev-parse --verify origin/integration` as an entry condition with its own STOP value. |
| B37 | blocker | L4-00 | T005 SELF-VERIFY:578-583 | path-ownership | The negative test creates and commits `reconciler/SEEDED-FOREIGN.txt` — an L3-owned path (PARTITION line 19) — on a `lane/4/*` branch, then at line 583 runs `rm -rf "$CP_ROOT/reconciler"` **unconditionally, after** the `reset --hard` has already restored the tree. In any cycle where `origin/integration` already carries L3's `reconciler/`, this deletes tracked L3 files. | Seed the canary in a scratch worktree or at an L4-owned-but-unexpected path; delete the unconditional `rm -rf`. |
| B38 | blocker | L4-05 | P5-14 check file, ~1408-1413 | executability | The permanent per-task check asserts a **transient** state (`INGEST ERROR missing-limb`). The moment P5-15 creates `ingest/collectors.py`, the grep fails, `set -eu` aborts, and `check.sh L4-P5-14` prints FAIL forever. P5-24's sweep requires it to PASS, so `sweep-phase5.sh` can never reach `PHASE5 OK`. | Rewrite the check to assert the post-P5-15 steady state, or move the negative into a one-shot fixture. |
| B39 | blocker | L4-05 | §6 D-L4-P5-04, ~316-323, and P5-01's `selected_mode: PENDING-D-L4-P5-04` | contradiction | The decision claims the spec never says which board mode is selected, and ships a validator that **fails if the mode is decided**. Spec line 9282 (§99.5) states the default outright: the single org-level Project with a product field is the default; per-product Projects with aggregation are the upgrade. | Delete the decision, set the default from 9282, and remove the `fail("selected-mode-decided-without-L0")` guard. |
| B40 | blocker | L4-05 | §0.1 ~120; P5-16 crit 7/8, P5-20 crit 7/8, P5-23 crit 7 | command-error | Windows CPython under Git Bash with bare `open(path)` decodes with cp1252, not UTF-8. The files carry U+2014 inside `arming_state`. Criterion 7 prints `32 False True` instead of `30 True True`; criteria 8 round-trip the files through `yaml.safe_load(open(p))` / `yaml.safe_dump(d, open(p,'w'))` and **mojibake the committed artifacts**. | Add `encoding="utf-8"` to every `open()` in every ad-hoc acceptance command in the lane. |

---

## 4. Non-blocking defects — summary

**82 major** and **59 minor** findings remain after deduplication. They cluster as follows.

- **C11 — branch model (5, major).** L4-00, L4-02 §6/T225, L4-03 line 5 and L4-07 §0.1/T01/T14 all run one branch per *phase*; PARTITION.md line 34 requires one per *task*, and each of these files declares PARTITION binding over itself — L4-02 line 7 even self-declares the difference a blocker for L0 and then never raises it. L4-07 contradicts itself internally: §4.3 prescribes `lane/4/07-${TASK}` while T14 pushes a single phase branch.
- **C12 — interpreter (2, major).** L4-02 and L4-05 mandate `.l4-venv` / `$L4_PY`; L4-04 and L4-07 mandate bare `python3` and explicitly refuse the venv. If the venv exists because system Python lacks PyYAML, L4-04's E4 fails on a runner-image blocker that Phase 2 already worked around.
- **Merge-gate scripts require unset environment variables (major).** L4-02 §8 promises four fail-closed status checks "taking no arguments beyond a mode"; all four open with `CP_ROOT:?` and `L4_PY:?`, the latter pointing at a path §3.2 says is never committed. A CI job exits with `unbound variable` and reads as a failing check rather than a missing precondition, so L2 cannot wire them in.
- **Self-verifies satisfiable without the work (7, major).** L4-02's `test-negative.sh` counts *any* non-zero exit as "rejected", so a wrong schema path prints `NEGATIVE 10/10`; L4-03's `DEPLOY-EMIT OK 6/6` and `WRITE-PATH COVERAGE OK 18/18` are literals over a seven-assertion harness and an eighteen-row register, and the coverage proof reports 18/18 for two stores whose only declared writer is unarmed; L4-04's `validate-store-naming.sh --selftest` reports its canary green when a missing `event-types.yaml` makes the temp run fail for an unrelated reason; L4-04's `CPRCLEAN=0` passes when `$CPR_ROOT` does not exist; L4-07's `run-tests.sh --all` never runs `suite-at.sh` or `suite-canary.sh`, so the command handed to the integration gate skips the acceptance-test proofs and the seeded canary entirely.
- **Judgment smuggled into executor tasks (8, major).** L4-04 invents seven self-report bands with representative hour values, a within-category truncation tiebreak, a 6-vs-12-month review cadence and a session-collision identity rule, all without decision ids; L4-06 P3-04 has the executor invent 17 freshness intervals, P7-01 roughly 60 breach-response policies, P3-07 a diagnostic-window value, and L4-T006 an "obligation" summary per spec section; L4-07 freezes another document's open output schema into "frozen" fixtures. All violate PARTITION line 47.
- **Repository-wide destructive or leaky commands (5, major/minor).** L4-04 runs `find "$CP_ROOT" -name '__pycache__' -exec rm -rf` across every other lane's tree, invisible to `git status` because the directories are untracked; L4-02 T204/T205 and L4-07 T13 copy corrupting fixtures into *tracked* paths with no `trap`, so an interrupt leaves a poisoned artifact that later tasks commit.
- **Remaining citation drift (~45, major/minor).** All are subsumed by B1's mechanical sweep, but several are individually dangerous because the wrong number lands on a real, different item rather than on blank space: L4-02 cites SIG-46 at 4575, which **is** SIG-42; L4-07's `at-map.yaml` cites AT-107 at 9377, which **is** AT-044; L4-06 cites §97.2 line 8890 for a rule whose text at that line is a literal YAML `id:` value.
- **Missing controls (12).** No FOREIGN check in 17 of L4-02's 25 tasks, despite §9 rule 5 telling the executor it is in "every self-verify"; L4-03 T301's entry condition E5 is proved nowhere; L4-04 T401's E1 and E5 are proved nowhere; L4-06 has no expected-output row for 87 of its 117 acceptance commands, and for six tasks the §4.3 command is a different command from the §2 acceptance command.
- **Scope and count errors (12).** L4-02 §2.1 undercounts its own deliverables by three files while `SCHEMAFILES=20` only works *with* one of them; L4-05's closing line says fourteen tasks over fifteen bodies; L4-07 promises five fixture corpora and builds nine; L4-99's headline "98 tasks have no body" contradicts its own evidence table; L4-06 builds `breach.schema.json` for which no Phase-7 task computes anything, so the two measures spec line 8865 names are built by no lane.

---

## 5. Verdict

### **BLOCKED**

Lane L4 cannot be dispatched. It fails on four independent grounds.

**1. It cannot start.** Whichever document an executor picks first, it halts inside the first three tasks. L4-02 halts at T202 (every valid fixture rejected — B5) or T203 (`SPEC=86` returns 1 — B1). L4-03 halts at T301 (`STORES=18` against a 19-marker skeleton — B4). L4-06 halts at task 2 of 117 (`gh api --jq -r` — B3), and would have halted at task 4 regardless (no clone — B13; selfcheck invoked before it exists — B14). L4-07 halts at T01 (five inventory artifacts that no task builds — B31). L4-01 has no task that can report PASS at all (55 `--jq -r` sites — B3), and its two most important command blocks are truncated mid-heredoc by fence collisions (B24).

**2. Two documents own the same paths.** Phases 2–7 are decomposed twice, incompatibly, and three ways for the RQM detector (B2). This is not an id-namespace nuisance: two agents on two branches write `tools/records/record-verification-result`, `metrics/attention/sources.yaml` and `tools/records/validate-schemas.sh` with different contents and incompatible interfaces. No lane-guard check can catch it, because both writers sit inside L4's own owned prefix.

**3. Its verifications do not verify.** The most-used self-verify in the lane — `check.sh <TASK-ID>`, the sole verification for 98 of 117 tasks — is satisfied by a two-line script that does nothing (B10). Five of L4-07's nine suites report green off usage errors (B6). The only mechanical enforcement of the no-hand-edit rule in the whole estate always passes (B18). The Phase-4 exit gate exits 0 on a broken phase (B16). The path-ownership proof at the Phase-7 push is skipped by a missing argument (B29). Exit 3 — the fail-closed state the lane is built around — is indistinguishable from exit 1 in the dispatcher every task runs (B15).

**4. It cites a spec that no longer exists.** All nine files were written against a 10,214-line revision; the spec is 10,231 (B1). Roughly 200 anchors are stale, six of them executable and halting, and the wrong numbers are transcribed into schema `description` fields, `store-map.yaml`, `at-map.yaml`, `sig-source-map.yaml` and a `CONTRIBUTING.md` that ships to `control-plane-records` for every future contributor. Two decisions (B28) and one acceptance-test contract (B33) are founded on spec text that does not exist at all — the SIG-43 collision was removed by D113, and §103.14 has never contained a "minimum baseline set".

The lane's **substance is largely sound**. In every case checked, the quoted spec text was accurate and the transcribed enums, taxonomies and field sets were correct — L4-02's 86-entry event taxonomy matches spec line 8963 entry for entry, including all eight compound entries. The failures are coordinates, interfaces and controls, not comprehension.

---

## 6. Shortest ordered fix list to reach READY

Nine steps. Steps 1–3 are L0 judgment and must complete before any sweep, because they determine which files survive to be swept.

| # | Step | Kind | Blockers cleared | Effort |
|---|---|---|---|---|
| **1** | **L0 picks one Phase 2–7 decomposition** and deletes or marks non-executable the loser's rows (L4-06 rows 20–117, or the task bodies in L4-02/03/04/05). Re-point L4-05's E2/E4/E7 and L4-04/L4-07's cross-references at the survivor. | **judgment** | B2 | 1 decision, then a delete |
| **2** | **L0 fixes six lane constants in one memo:** (a) one workspace root and variable-name set; (b) one interpreter convention; (c) one records-repo creation path; (d) the store-marker count (17 stores + `events/`, and whether `bootstrap/` is tracked); (e) one branch model — amend PARTITION line 34 or split the phases; (f) renumber `D-L4-01/02/03` on one side. | **judgment** | B4, B25, B26, B27, C11, C12 | 1 memo |
| **3** | **L0 freezes four Phase-0 contracts** under `contracts/`: the writer CLI (`record-write` / `event-append` / `record-verification-result`); the `event_type` identifier list and payload field sets (~95 names); the `sig-source-map.yaml` source semantics; and the `derive.py` output schema. | **judgment** | B6, B17, B32, plus L4-07's frozen-fixture and AT-044 shape problems | 4 contracts |
| **4** | **Mechanical citation sweep** across all nine files and every heredoc: re-derive every anchor against the 10,231-line spec, correct the five `10214` constants, and convert bare line numbers into `sed -n '<N>p' \| grep -q '<literal>'` assertions. Then delete D-L4-TL-4 and D-L4-P7-02, set `signal_id: SIG-47` with a list-valued source, unblock L4-06 P3-06/P7-14, and delete L4-07's fabricated AT-046 minimum set. | **mechanical** (the list-valued source is a one-line schema change; the AT-046 deletion needs L0 to confirm no minimum set is wanted) | B1, B28, B33 | 1 scripted pass + review |
| **5** | **Mechanical CLI sweep:** delete 73 stray `-r` / `-c` / `-S` after `--jq`; replace 6 `--repo-dir` with `--repo "$REPO_SLUG"`; replace both broken slug `sed`s with the strip-then-extract form and hoist `$REPO_SLUG`; add `encoding="utf-8"` to every ad-hoc `open()`; re-fence L4-01's two heredoc-in-fence blocks. | **mechanical** | B3, B21, B22, B24, B40 | 1 scripted pass |
| **6** | **Environment and ordering sweep:** add the missing `CPR_ROOT` exports to L4-03's four blocks and the `l4_guard`+exports prologue to L4-07 T02–T14; add clone-or-STOP for both repos to L4-06 §0.1; move L4-02 T201's `test -d` guards after the `checkout -B` and add `schemas/records` and `metrics/taxonomy` to its `mkdir -p`; move L4-06's six harnesses ahead of their consumers; exempt L4-T004/T005 from the selfcheck line and add `--coverage` to the charter script; add `--paths` at L4-04:3518; replace every `checkout -B` on a shared branch with `switch -c` plus an `origin/integration` precondition; add a Phase-0 `gh label create` task and an ORG-independent blocker fallback. | **mechanical** | B11, B12, B13, B14, B19, B23, B29, B35, B36, and L4-02's T201 preflight defect | 1 pass, ~40 edits |
| **7** | **Repair the writer / schema / taxonomy contracts** against the step-3 freeze: rename L4-03's three `board-events` ids to the L4-02 taxonomy and make `validate-board-events.sh` parse `id:` rather than grep free text; add `write_path` / `deployed_by` (or build bodies from `required`) so emitted records validate; reconcile `DECP` vs `^DEC-`; make the L4-02 validator drop PyYAML's timestamp resolver. | mostly **mechanical** once step 3 exists; the `DECP` choice is **judgment** | B5, B8, B9 | ~1 day |
| **8** | **Repair the controls:** constrain `checks/<TASK-ID>.sh` to invoke its own acceptance command plus a negative fixture, and add `check.sh --audit`; propagate exit 3 through L4-06's dispatcher; split the P4 exit gate; rewrite L4-03 T315's guard to take the diff as an argument; rename `RECORDS_ROOT` to `CPR_ROOT` in L4-07 and assert the sandbox received the writes; branch and PR the records repository in L4-03; add `suite-at` and `suite-canary` to `run-tests.sh --all`; fix `INPUTS`; fix L4-05 P5-14's transient check; remove L4-00 T005's cross-lane canary; add the missing FOREIGN and entry-condition rows. | **mechanical** | B7, B10, B15, B16, B18, B20, B30, B34, B37, B38, plus the major "self-verify passes without the work" cluster | ~1 day |
| **9** | **Assign owners for the five orphan artifacts** L4-07's entry gate requires but no task builds (`read-across`, `validate-freshness.sh`, `validate-retention.sh`, `write-freshness.yaml`, `validate-sources.sh`), then add `rqm-detect` / `rqm-emit` to the gate and set `ENTRIES=28`. Resolve D-L4-P5-04 against spec line 9282. | **judgment** (which phase owns them), then mechanical | B31, B39 | 1 decision + 5 task bodies |

**Mechanical vs judgment split.** Of the 40 blocking entries, **27 are fully mechanical** once steps 1–3 land (steps 4–8 are scripted sweeps and mechanical edits); **9 require an L0 decision** (B2; B4/B25/B26/B27 as one memo; B6; B17; B31; B32; B39); and **4 are mixed** (B5's fix is mechanical but must not be delegated to an executor, since the file's own standing rule would steer an agent into rewriting seventeen fixtures instead; B8, B33 and B34 need one-line L0 rulings before the edit). Roughly **70% of the blocking mass is a sweep**; the remaining 30% is nine decisions that fit on two pages.

**Do not dispatch any part of L4 until steps 1–3 are published.** Steps 4–6 are safe to run in parallel across all nine files once step 2's constants are fixed.

---

*Consolidated from 257 raw findings across two review rounds. Sixteen claims re-verified against the live files and the live 10,231-line spec; none refuted, one narrowed.*
