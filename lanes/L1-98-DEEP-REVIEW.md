# L1 — CONSOLIDATED LANE REVIEW (rounds 1 + 2)

**Scope.** All nine Lane-1 documents: `L1-00-charter.md` (828), `L1-01-repo-skeleton.md` (1,957), `L1-02-schemas.md` (2,226), `L1-03-validators.md` (2,029), `L1-04-ci-gate-engine.md` (1,728), `L1-05-tasks.md` (6,132), `L1-06-tests.md` (1,328), `L1-07-runbook.md` (1,132), `L1-99-review.md` (547) — 17,907 lines. Round-1 findings for L1-00 and L1-01 were harvested from `review-harvest/_ALL_DEFECTS.round1-backup.json` (50 defects) and merged with the round-2 per-file findings for L1-02..L1-07 and L1-99.

**Counts after dedup.** 30 blocking families (from 66 raw blocker findings — 36 were duplicates of a cross-file family), 71 major, 44 minor.

---

## 1. SPOT-CHECKS OF THE MOST SERIOUS CLAIMS

I re-ran the highest-consequence claims myself. The three most serious survive. Two further checks are recorded because they overturn claims made *inside* the lane.

| # | Claim | Command | Result |
|---|---|---|---|
| SC-1 | Five incompatible schema path conventions inside one lane | `grep -ohE "schemas/(registry|product)/[A-Za-z0-9._/-]*\.schema\.json" lanes/L1-0{1,2,3,5,6}*.md \| sort -u` | **CONFIRMED.** L1-01 → `schemas/registry/common/<n>.schema.json`; L1-02 → `schemas/<area>/<kind>/v<N>/<kind>.schema.json`; L1-03 → `schemas/registry/<kind>.v<N>.schema.json` + `capability.enum.schema.json`; L1-05 → `schemas/registry/<kind>.v1.schema.json`, `schemas/product/product.contract.v2.schema.json`; L1-06 → flat `schemas/<area>/<id>.schema.json`. No two agree. |
| SC-2 | Four mutually exclusive "frozen, only interface" validator contracts | `sed -n '30p' L1-03`; `sed -n '355,375p' L1-05`; `sed -n '91,99p' L1-04`; `grep -n 'LANE1 SUITE' L1-06`; `grep -rln 'REGISTRY-VALIDATORS' lanes/` | **CONFIRMED.** L1-03 §0 freezes `python -m validators.registry.cli --root … --as-of … --format json`. L1-05 L1-003 freezes `python -m validators.registry.cli validate [--file] [--dir] [--today]`, success line `OK: <n> file(s) validated, 0 error(s)`, and states "This CLI is the **only** interface Lane 2 may call." L1-04 §1.3 freezes `python3 <rule-dir>/check.py --root <target-root>` emitting `FINDING …`. L1-06 freezes `run.sh --all` → `LANE1 SUITE: PASS`. L1-07 freezes `run-all.sh` → `REGISTRY-VALIDATORS: PASS`; that token appears in **no other file in the corpus**. |
| SC-3 | `validators/registry/run-all.sh` is created by no task | `grep -rn 'run-all' lanes/ master/ protocol/ manual/ PARTITION.md` | **CONFIRMED.** 18 hits in `L1-07-runbook.md`, 2 in `L1-99-review.md`, zero in L1-00..L1-06. L1-07 line 21 asserts "Created by lane task L1-01"; `grep -c run-all lanes/L1-01-repo-skeleton.md` = 0. RB-00's fail-closed STOP therefore fires for every L1 task in every session. |
| SC-4 | L1-99 D1: "L1-05-tasks.md is truncated, 30 of 61 task bodies missing" | `wc -l lanes/L1-05-tasks.md` = 6132; `grep -cE '^### L1-[0-9]{3} '` = 61; L1-401 @2015, L1-404 @2338, L1-505 @2993, L1-605 @3827, L1-704 @4853, L1-901 @5528, L1-905 @5949 | **REFUTED.** All 61 bodies exist. L1-99's first BLOCKING defect, its verdict banner, its "127/127 control coverage" figure and MINIMUM SET row 3 are all computed against a superseded 2,003-line copy. |
| SC-5 | L1-99: "the citation layer is sound — 25/25 anchors resolve, zero ANCHOR-DRIFT" | `grep -n -m1 -F '## Section 20' $SPEC` etc. | **REFUTED — and the underlying round-1 defect CONFIRMED.** §20 = 2029 (pinned 2027), §31 = 2743 (2741), §52.6 = 4618 (4613), §99.2 = 9195 (9184), §100 = 9306 (9295), §101 = 9454 (9443). 20 of 25 anchors are wrong; `L1-00-04/check_spec_anchors.sh` fails deterministically. Also verified: the spec is 10,231 lines (L1-00 line 8 says 10,214); §52.6 line 4620 reads "the complete inventory — **thirty** entries"; `grep -oE '^\| SIG-[0-9]{2}' \| sort -u \| wc -l` = **47**; D113 exists at line 10227 and records the SIG-43 → SIG-47 renumber. |
| SC-6 | L1-02 `check.mjs` defects (claimed verified-by-execution) | read `lanes/L1-02-schemas.md:183-227` | **CONFIRMED by inspection.** `walk()` recurses `for (const [k,v] of Object.entries(node)) walk(v, …)` into every key including `if`/`then`/`allOf`, and applies the `x-origin` check to any node carrying `.properties` — so every applicator subschema is flagged. `makeAjv()` unconditionally `addSchema(common)`; `validateOne` then `ajv.compile(readJson(schemaPath))`, which throws `schema with key or id … already exists` when the target *is* common.schema.json — exactly T02's two fixture runs. |
| SC-7 | L1-04 `$CONTROL_PLANE_ROOT` unset | `grep -c CONTROL_PLANE_ROOT lanes/L1-04*.md` = 11; `grep -rn 'CONTROL_PLANE_ROOT=' lanes/` | **CONFIRMED.** Assigned only in L2 documents. L1-01 exports `CP_ROOT` instead. `cd ""` succeeds silently. |
| SC-8 | L1-04 depends on `rule.yaml`, which no task creates | `for f in lanes/L1-0[1-6]*.md; do grep -c 'rule\.yaml' $f; done` | **CONFIRMED.** 19 hits in L1-04, 0 in L1-01/02/03/05/06. |
| SC-9 | L1-06 §6 AT count | `grep -c '^| \*\*AT-' lanes/L1-06-tests.md` = **24**; line 1269 asserts "23 AT ids" | **CONFIRMED.** T14 hard-codes 23 in two criteria and both SELF-VERIFY tests; T14 is unpassable and T15 depends on it. |

---

## 2. BLOCKING DEFECTS (deduplicated, ranked by execution blocked)

B1–B8 are cross-file: each halts two or more documents at their first task. B9 onward are file-local.

| # | Sev | File(s) | Location | Cat | Problem | Fix |
|---|---|---|---|---|---|---|
| **B1** | blocker | L1-01, L1-02, L1-03, L1-05, L1-06 | L1-01:735-744; L1-02 whole; L1-03:234-262, 641, 702; L1-05 all schema tasks; L1-06:51-81, 229-236 | contradiction | **Five incompatible schema path conventions** (SC-1). L1-03-00 is fail-closed on `schemas/registry/capability.enum.schema.json` and `schemas/product/product.v{1,2}.schema.json`, which no task creates — blocking T01..T20. L1-06-01 proves the set with `find … -maxdepth 1` against 18 flat ids that can never appear — blocking all 15 of its tasks. L1-02-19's `find … -not -path '*/_common/*'` also sweeps in L1-01's `schemas/registry/common/` tree. | L0 publishes ONE `contracts/**` schema-path concordance (path grammar, `$id` host, `_common` vs `common`); L1-03-00/T02, L1-05, L1-06 §2/T01 and L1-02-19 are rewritten against it. |
| **B2** | blocker | L1-03, L1-04, L1-05, L1-06, L1-07 | L1-03:30; L1-04:91-99; L1-05:355-373; L1-06:237-258, 322-334; L1-07:21, 213-266 | contradiction | **Four mutually exclusive "frozen" validator/rule contracts, each declaring itself the only interface** (SC-2), plus two disjoint rule-id namespaces (`r01_schema_version.py`…`r18` vs 84 ids of the form `R-CAP-01`) writing into the same `validators/registry/rules/`. L1-06's `l1_validate` (`--schema {ID} --file {FILE}`) cannot be built from any of them; L1-04's `run_rule` executes a `check.py` no rule has. | L0 publishes one CLI grammar, one rule-module shape, one rule-id grammar and one `requirements.txt` under `contracts/**`; losing documents are marked superseded and reissued. |
| **B3** | blocker | L1-07 (+L1-01) | L1-07:21, 92, 116, 135, 216, 243, 346, 422, 443, 631, 652, 714, 1037 | completeness | **`validators/registry/run-all.sh` is created by no task** (SC-3), yet it is the sole entrypoint of 10 of 14 runbook procedures and of RB-00's fail-closed preflight. Every L1 agent files a `SETUP` blocker on its first command, forever. Secondary: RB-02 is satisfiable by a 3-line script printing the expected strings — no fixture floor, no negative control, no provenance check. | Add a task that authors it to the exact RB-02 contract, or (preferably, under B2) rewrite L1-07 against the single L0-frozen entrypoint. Add an anti-fabrication control to RB-02: a fixture-count floor plus a seeded-defect negative control. |
| **B4** | blocker | L1-04 | 79-89, 503, 613-625; 564, 596-604; 428-436 | completeness / contradiction / command-error | **L1-04 cannot start and its first mutating command is destructive.** (a) The engine requires `rules/<id>/rule.yaml`; no task in the corpus creates one (SC-8), so T-02's audit prints `RULES-FOUND 0` and STOPs. (b) `ENGINE_ENTRIES` raises `LayoutError` on any entry under `validators/registry/` outside 8 names, so on the tree L1-03/L1-05 build (`cli.py`, `loader.py`, `tests/`, `tools/`, `RULES.md`, …) `gate.py` returns exit 2 on every run forever. (c) T-02's sweep loop `git mv`s `tests/` (220 referenced paths) and `tools/` into `rules/`, before any check runs, with no dry-run. | Reconcile under B2. Immediately: add `tests\|tools` to the skip list, gate the loop behind a dry-run, widen or scope `ENGINE_ENTRIES`, and either generate `rule.yaml` from `rules_manifest.yaml` or file a CCR. |
| **B5** | blocker | L1-02, L1-03, L1-05, L1-06, L1-99 | L1-02:1423,1455,1461,1478,1659; L1-03:54,1341,1350,1925; L1-05:119-122,3019-3052,3867-3876,4855-4880,5680; L1-06:49; L1-99:231,246,505-512 | citation | **Pre-amendment spec counts compiled into shipped artefacts and into unsatisfiable STOPs** (SC-5). Spec: 47 signals, 30 control-plane artifacts, D1..D113; SIG-43 = "Founder operating load", the unclosed-learning-loop signal is SIG-47. The lane asserts 46/29/D1-112/SIG-43. L1-05-L1-605, L1-505 and L1-902 carry **unconditional STOPs demanding the executor quote sentences that no longer exist** — unsatisfiable, the agent stalls. L1-704 ships `declared_count: 29` and institutionalises the resulting failure as "known-red", which L1-904 check 4 then asserts must keep firing. L1-02-09 writes `SIG-01..SIG-46` into a shipped `x-residual-rule`; L1-02-12 mis-cites SIG-43. | Sweep 46→47, SIG-46→SIG-47, 29→30, D1-112→D1-113, and SIG-43→SIG-47 at L1-02:1659. Then delete L1-605's STOP, L1-704's known-red framing and L1-904 check 4's R-INV-01 canary, and re-derive L1-06 §2's 18-id set from the real thirty-row §52.6. |
| **B6** | blocker | L1-00; certified PASS by L1-99 | L1-00:578-603, 632; L1-99:20, 30, 499-501 | citation | **20 of 25 pinned spec anchors are wrong**, so `L1-00-04/check_spec_anchors.sh` emits 20 `ANCHOR-DRIFT` lines and `SPEC-ANCHORS-FAIL` on its first run, and its STOP halts the lane's first phase. L1-99 then certifies this exact check as **PASS, 25/25** (SC-5), so an integrator working from the review skips the repair. | Re-pin all 25 rows to the measured values (§20 2029, §21 2115, §26.4 2528, §31 2743, §38.1 3375, §44.1 3965, §52.6 4618, §54 4753, §55 4837, §60 5126, §62.1 5296, §64.1 5440, §66.2 5515, §68.1 5631, §77 6346, §91.2 8087, §96.2 8730, §99.2 9195, §100 9306, §101 9454); record the spec SHA-256; correct L1-00 line 8 to 10,231; downgrade L1-99 check 6 to FAIL. |
| **B7** | blocker | L1-00, L1-01, L1-03, L1-04, L1-05, L1-06, L1-07 | L1-01:229; L1-03:143; L1-04 (11 sites); L1-05:40-52, 256; L1-06:136 + 11 sites; L1-07:69-72 | executability | **No task creates or clones the repository, and four different root variables are used.** `CP_ROOT` (L1-01) vs `$CP=$HOME/work/control-plane` (L1-03) vs `$CONTROL_PLANE_ROOT`, never set anywhere in L1 (L1-04, 11 sites — SC-7) vs `$REPO_ROOT`, never set in L1-06 (12 sites) vs unchecked `git rev-parse` (L1-07). `cd ""` succeeds silently, so `git checkout -b`, `mkdir -p` and 40 `python3` calls run in an arbitrary directory with no error. L1-03:143 clones `git@github.com:<org>/…` with `<org>` unresolved. L1-05-L1-001's first command is `cd "$REPO_ROOT"; git fetch origin` against a repo that does not exist. | One `contracts/**` bootstrap fact: repo URL, org login, clone path, one variable name. Every L1 document uses that name and opens each block with `set -euo pipefail` plus `[ -d "$X/.git" ] \|\| exit 1`. |
| **B8** | blocker | L1-05 (+L1-03, L1-06) | L1-05 §0.2:50 and all 61 COMMANDS blocks; L1-06 §8:1289-1306; L1-03 §4 | executability | **Every task branches fresh from `origin/integration` while consuming files an earlier, unmerged task authored.** PARTITION fixes merges as a once-per-cycle train, so task N+1's `cp`/`import` finds nothing. Affects ~40 L1-05 tasks (L1-003 imports L1-002's `report`; L1-402 copies L1-401's fixtures; L1-503 copies `roles.yaml`/`topology.yaml`; L1-702 copies `platform.yaml`) and 10 of 15 L1-06 tasks. No STOP rule covers it. | L0 picks: (a) stacked branches (`git checkout -B lane/1/<id> lane/1/<prev>`), one train slot for the lane; or (b) a written continuous-merge exemption plus a per-task `git pull --ff-only` gate and a STOP when a dependency's FILES are absent. |
| **B9** | blocker | L1-02 | 183-212 (`lint()`) | executability | `lint()` demands `x-origin` on every child of every node carrying `properties`, **including applicator subschemas under `if`/`then`/`else`/`allOf`/`anyOf`** (SC-6). Its own two literal schemas fail (common: 2 LINT-FAIL; people: 10). T02 criterion 2 and T03 criterion 1 are unreachable, and every SELF-VERIFY in the file is `&&`-chained on `--lint`. T05/T10/T14/T17/T18 all require conditionals. Phase-2 exit criteria 3-5 can never be met. | Skip applicator keywords when recursing: `const APPLICATOR = new Set(['if','then','else','not','allOf','anyOf','oneOf','dependentSchemas','propertyNames','contains'])`; do not run the annotation check on nodes reached through them. |
| **B10** | blocker | L1-02 | 214-227; T02 criteria 6-7 (561-562); commands 538-547 | command-error | `makeAjv()` pre-registers common.schema.json unconditionally; when the schema under test *is* common.schema.json — T02's own two fixture runs — Ajv throws `already exists`, EXIT=2 (SC-6). Neither criterion 6 (`VALID`, exit 0) nor 7 (`INVALID`, exit 1) can pass. With no `set -e`, exit 2 produces no output and execution falls straight through to `git add`/`commit`/`push`/`gh pr create` — a broken harness ships and opens a PR. | `makeAjv(skipPath)` skipping registration when `path.resolve(commonPath)===path.resolve(skipPath)`; add `set -euo pipefail` to every command block and guard each validation before any `git add`. |
| **B11** | blocker | L1-02 (+L1-01) | T19:2085-2100; Phase-2 exit 1:2214 | contradiction | T19's `EXPECTED=16` with filter `-not -path '*/_common/*'` returns **18** where L1-01 has merged (`schemas/registry/common/effective-dating.schema.json`, `registry-envelope.schema.json` — no underscore), so the STOP fires permanently. Patch the count and the index generator sweeps both files in; they carry zero `x-origin`, zero `x-residual-rule`, no `fixtures/`, and a `$id` host of `control-plane.invalid` against this file's `control-plane.internal`. | Fold under B1. Minimum: exclude `schemas/registry/common/` too and add an entry-gate assertion; better, CCR that L1-01's tree be folded into `_common/v1/` under one `$id` host. |
| **B12** | blocker | L1-02 | T05-T18 (14 tasks); SELF-VERIFY at 1547, 1609, 1667, 1770, 1816, 1883, 1991, 2057 | executability / judgment-required | **Fourteen of nineteen tasks supply no literal artefact content** — T01-T04 give complete heredocs, T05 onward say "follow the same shape as T03". T05 alone requires authoring a ~25-block JSON Schema with 8 conditionals and 13 residual-rule strings from a prose table, plus a 240-line YAML fixture and five negatives. Their SELF-VERIFY blocks then assert internal structures the body never specifies (`$defs.changeEntry.properties`, `s.properties.load_model.properties.factors.properties`), so a correct-but-different model fails its own check and must STOP. PARTITION: "No task may require designing, choosing, or interpreting." | Either supply the literal heredoc for every T05-T18 as T01-T04 do, or demote T05-T18 to L0 authoring and have L1 transcribe frozen `contracts/**` artefacts. |
| **B13** | blocker | L1-03 | T07:957 (R06.3); T09:1145 (R08.2) vs baseline:541; T02:641 (R01.4) vs baseline:328-341 | contradiction | **Three rules break the `_baseline` fixture the moment they merge.** R06.3 fires unconditionally when `--records-root` is absent and product-1 has a `recovery:` block. R08.2 fires because the baseline is `conflict_check: waived-with-decision` with no `waiver_decision_record`. R01.4 fires on `roles.yaml` (no `supported_contract_versions` key) and on `people.yaml` (key is `people_registry`; no kind→key map is published). T01 c1, T02 c4, T03 c4, T04 c3 and T20's exit-contract row all require `stdout exactly []`. Because T02-T19 run in parallel on separate branches, this is discovered only after merge. | Publish the kind→key map in T02 and exempt `roles.yaml` from R01.4; add `waiver_decision_record` to the T01 baseline and have r08's fixture delete it; scope R06.3 to one info-severity finding keyed on the root and exclude `info` from `assert_expected`. All three must land in T01/T02 before T03+ are dispatched. |
| **B14** | blocker | L1-03 | T01 Step 8:574; T16:1655-1662; T18:1795; T13:1435-1439; T03:742-750 | executability | **Single-delta fixtures trip multiple rules, and `assert_expected` compares the code set exactly.** r15/fail-departed-not-revoked fires R02.2 three times against `expect:["R15.1"]`; r15/fail-revoked-not-departed fires R02.3; r17/fail-no-end-date fires R05.2; r12/fail-founder-people-intelligence fires R12.1 as well as R12.2; r02/fail-departed-person fires three R02.2 plus R02.3. | Fix in T01 before parallel dispatch: either `run_cli` passes `--rule <rid>` and T20 assertion 4 is restated accordingly, or every fixture's `expected.json` lists the full cross-rule code set. Correct each affected `expect` in the same edit. |
| **B15** | blocker | L1-03 | T05:833-846; T09:1174-1177; T19:1846-1849; T01 SELF-VERIFY:615 | command-error | **Four fixture-construction commands produce invalid YAML or the wrong tree, and the keystone self-verify cannot pass.** `cat >>` appends a 2-space sequence item after `rotas: []` (r04, all 5 fixtures exit 2); appends a 4-space key after `revenue_importance: high` (r08/pass-waived, exit 2); appends `restore_production_workflow` into `business:` instead of `recovery:` (3 of 5 r18 fixtures wrong). T01's SELF-VERIFY runs a command *required* to exit 2 under `set -e`, so the block aborts and `T01_PASS` never prints. | Replace the three appends with python splices (the pattern T03/T06/T14/T18 already use). Rewrite T01's exit-2 check as `rc=0; cmd \|\| rc=$?; test "$rc" -eq 2`. |
| **B16** | blocker | L1-03 (+L1-02) | T00 c3:248; T02 STOP:702 vs L1-02:1265 | contradiction | T00 criterion 3 and T02's STOP both require `schemas/product/product.v1.schema.json` to exist. L1-02 line 1265 records **DECISION-L1-02-A as open** and states "no v1 product contract shape appears anywhere in the specification … this phase ships v2 only." The v1 schema is guaranteed absent; the STOP is guaranteed to fire. | L0 resolves DECISION-L1-02-A before T00 is dispatched. If v2-only: R01's §60.2 both-versions demonstration moves to a `people.yaml` `registry_version` fixture pair and `supported_contract_versions.product` becomes `[2]`. |
| **B17** | blocker | L1-03 vs L1-05 | L1-03-11:1276, 1326-1328 vs L1-05-L1-603:3599, 3609, 3687 | contradiction | Both tasks create `registries/policies.yaml` with mutually exclusive content: L1-03-11 seeds the §64.2 control rows and asserts 12 ids; L1-05-L1-603 ships `policies: []` and asserts `POLICIES=0`. Whichever runs second overwrites the other or fails its own verification. | L0 assigns one owner. If L1-03 keeps it, delete the path from L1-603's FILES and change its criterion 5; if L1-05 keeps it, T11 seeds only its fixtures. |
| **B18** | blocker | L1-03 | T15:1596-1625 | executability | T15 (Size L, six fixtures, the two-revision integrity rule) describes the `_base/` subtrees the rule reads in one prose sentence — no content, no deltas, no `expected.json` — while acceptance criterion 2 runs the CLI against a `_base` directory the task never creates, and its STOP forbids authoring the framework-registry content four of the six protected entity kinds live in. It also amends the frozen §0 CLI with two new flags (`--base`, `--base-dir`) post-freeze. | Add literal commands for all six `_base/` trees, deltas and `expected.json`, restricted to people/roles/access-rules; add the two flags to §0 and implement the mutual-exclusion exit-2 rule in T01. Until then dispatch T15 as BLOCKED. |
| **B19** | blocker | L1-04 | 37 vs 105-106, 320-322, 563, 751, 782 | contradiction | The file's own §0 obligation quotes §53.4 correctly ("only Blocking blocks work; Red is material but non-blocking") and then hard-codes `BLOCKING_SEVERITIES = ("red","blocking")`, so every Red registry finding blocks a merge the spec says must not be blocked. The shipped README states both positions on one page. Verified against §53.4: Red → "Blocks work: **No**". | `BLOCKING_SEVERITIES = ("blocking",)` in discovery.py:563 and report.py:751; correct lines 105-106 and README:322. If Red really must block, that is an L0 deviation decision, not a lane choice. |
| **B20** | blocker | L1-04 vs L3 | 1180, 1191-1206, 1226, 1673 vs L3-03:344, 363-397 | contradiction | Two permanent seeded canaries, both citing §53.1/AT-102/D63, under two owners at two severities: L1's `registries/canary/seeded-canary.yaml` (`gate-selftest-canary-001`, amber) and L3's `validators/drift/canary/canary.yaml` (`CANARY-DRIFT-001`, green). L3-03 line 24 states AT-102 "is proved by task T14 and by no other task"; L1-04-T-09 criterion 3 also claims it. §53.1 says "A **permanent** seeded drift record … exists at all times" (singular). | L0: the §53.1/AT-102/D63 canary is L3's. Rename L1's to a gate self-test probe citing §31.2/D97 only, and strike AT-102/D63/§53.1 from L1-04 lines 36, 1180, 1191-1206, 1226, 1673. |
| **B21** | blocker | L1-04 | T-01 STOP:406 | contradiction | T-01's STOP forbids overwriting an existing `validators/registry/README.md` or `gate.py` with different content. `README.md` is authored by three tasks across the lane (L1-00-02, L1-04-01, L1-05-L1-903) and `requirements.txt` by two. On the merge train the STOP fires on the phase's very first task. | T-01 appends a delimited `<!-- L1-04 ENGINE CONTRACT -->` section and merges `requirements.txt` rather than `cat >`. L0 reconciles the three README authorships. |
| **B22** | blocker | L1-05 | §0.5:500-510 vs L1-505:3055, L1-605:3908, L1-606:4085, L1-607:4260, L1-608:4400, L1-701:4548, L1-704:4900, L1-801:5160, L1-802:5300, L1-902:5710 | contradiction | L1-005 freezes the fixture-case contract at **nine** recognised document filenames. Ten later tasks build cases whose documents are `os-health.yaml`, `tools.yaml`, `economics.yaml`, `platform-roadmap.yaml`, `patterns.yaml`, `OWNERS.yaml`, `INVENTORY.yaml`, `framework*.yaml`, `invariants.yaml`, `product-v{1,2}.yaml`. No task extends `load_fixture_case`. All ten SELF-VERIFY blocks fail at fixture load. | In L1-005, replace the fixed list with a rule ("basename minus `.yaml` is the kind; `<kind>-<n>.yaml` for multiples") and restate its tests. Declare it once. |
| **B23** | blocker | L1-05 | §0.3:60-72 vs L1-108:963, L1-202:1092-1110, L1-204, L1-205, L1-207, L1-305, L1-306, L1-307, L1-308 | completeness | §0.3 orders `git add <exact paths in FILES>` and `git restore --staged` anything else. **Nine tasks author sibling modules or edit files their FILES field never lists** (L1-202 names `r_cap_02.py` but writes `r_cap_03.py`/`r_cap_04.py`; L1-308 defines R-PRD-20..26; L1-108 edits `defs.v1.schema.json`). Under §0.3 those are un-staged and the PR ships tests importing uncommitted modules. L1-402 proves the authors saw the pattern and fixed only that one task. | Extend every affected FILES field to list every module and edited file, as L1-402 does; add to §0.3: "a task whose body names a file its FILES field omits is a defect — file a blocker, do not guess." |
| **B24** | blocker | L1-05 | L1-301:1367 vs L1-303..L1-308, L1-703; L1-403:2275-2295 | contradiction | Tasks L1-301→L1-308 and L1-703 each add new **required** top-level blocks to `product.contract.v2.schema.json`, and no task regenerates the fixtures earlier tasks authored. L1-301's `service_minimal`/`novel_class` become invalid at L1-303; L1-403 then copies exactly that stale fixture and asserts `0 error(s)`. L1-904's `698 passed` is unreachable. | Give L1-303..L1-308 and L1-703 an explicit "regenerate every earlier `product/**` valid fixture from the canonical `valid/service_full/product.yaml`, and re-run every prior product test" step; or introduce blocks as optional and flip them all to required in one closing task with a fixture regeneration. |
| **B25** | blocker | L1-05 | L1-503:2760-2823; L1-802:5232; L1-904 check 4:5877 vs L1-202:1120-1126 | contradiction | `registries/roles.yaml` is a verbatim §8 transcription that L1-202 proves emits **three** R-CAP-02 errors. L1-503 and L1-802 `cp` it into fixtures declared "Expected errors: none" and assert `0 error(s)`. L1-904's lane-certifying check 4 asserts the live error set is exactly **two** known-reds; it is four. The script that certifies the lane fails on its first run. | Both fixture tasks copy the sanitised `fixtures/roles/valid/full/roles.yaml` (L1-102), or their `expect.yaml` lists the three errors. Restate L1-904 check 4 as four KNOWN-RED lines and fix acceptance row 5 from `2` to `4`. |
| **B26** | blocker | L1-05 | L1-310:2016-2062 vs L1-404 ORDERING NOTE:2358-2360 | executability | L1-310 (row 30) implements R-REF-03 against `registries/services/*/service.yaml`, a tree created at row 35 by L1-404. The warning exists only inside L1-404, five tasks later. §0 rule 4 then stalls the executor: L1-311 depends on L1-310, so there is nothing to move on to. | Move L1-404 above L1-310 (its only dependency is L1-302, so the move is legal), renumber the index rows, delete the ORDERING NOTE. |
| **B27** | blocker | L1-05 | L1-904:5878, 5897, 5915, 5925; L1-905:6027; §3 proof 5:6071 | command-error | Lane path-ownership is proved with `git diff --name-only origin/main...HEAD` filtered to L1's four trees, expecting `0`. A branch cut from `origin/integration` contains every other lane's merged work, so the count is non-zero through no fault of L1 — and L1-904's STOP says the branch "must not be pushed". The lane cannot hand off. | Use `origin/integration...HEAD` in L1-904; for L1-905/§3 proof 5 scope by commit (`git log --format=%H origin/main..integration --grep='^L1-'`, then `git show --name-only`). |
| **B28** | blocker | L1-06 | §6:1244-1269; T14:1163, 1165, 1182-1183 | completeness | §6 contains **24** AT rows; line 1269 and T14's criteria 1 and 3 and both SELF-VERIFY tests hard-code **23** (SC-9). T14's STOP forbids the only two escapes ("do not invent an AT id, do not renumber"). T14 is unpassable and T15 depends on it. | Change 23→24 in all four places, or delete one AT row and say which. |
| **B29** | blocker | L1-06 | T11 mutate.sh:970-976 | command-error | **The mutation harness cannot detect a mutation.** Line 970 is a no-op loop over the valid corpus; the real loop iterates fixtures that are already invalid, so `detected=1` regardless of the mutation; and the mutated schema root is passed as `L1_SCHEMA_ROOT`, which `l1_validate` never reads — the validator always reads the real `schemas/`. For the ~10 ids with no invalid corpus `detected=0`, the floor is breached, `mutate.sh` exits 1, and because `run.sh --all` invokes it, **`run.sh --all` can never exit 0** — breaking T12 c5, T13 c5 and T15's SELF-VERIFY. | Delete line 970; iterate `fixtures/valid/$id/*.yaml` asserting a previously-passing fixture now fails; add a `{SCHEMA_ROOT}` placeholder to `validator.cmd` (frozen in T01) so the mutated root actually reaches the validator. Do not dispatch T11 until then. |
| **B30** | blocker | L1-06 | T13:1073-1104 | missing-control | Layerability half 2 ("with `exceptions` also absent the run MUST fail") is a tautology: the `rm -rf` runs after the validation loop; the `[ -d ]` check is always false immediately after it; the `grep -q` reads a static file the task never modifies. It always prints PASS — in a document whose §1 thesis is that an instrument that cannot fail is not an instrument. | Remove `exceptions` from both the copied corpus and a copy of `schema-ids.txt`, re-run the loop, require non-zero exit; or delete half 2 and say so. |
| **B31** | blocker | L1-06 | T03:481-482, T04:532, T06:682-684; whole file (spec path) | executability | **76 of the 174 promised files have no obtainable content.** The three largest tasks replace their bodies with a comment ("author minimal.yaml and typical.yaml per the content rules above"), the content rules say "verbatim transcription of the spec's own worked example", and the document **never names or paths the specification** (`grep -n 'MasterSpec\|Research/' L1-06` → nothing). T03's criterion 4 would be satisfied by 30 empty files. | Add the literal spec path to §4 and give every "verbatim" instruction a `sed -n 'A,Bp' <path>` with real line numbers; add per-file heredocs and a non-emptiness criterion. |
| **B32** | blocker | L1-07 | 285, 347, 431, 559, 655, 713, 933, 1014 | command-error | **The lane guard fails OPEN.** `L1_ALLOW` is exported only in RB-00; RB-02..RB-13 are separate copy-pasteable blocks. With it unset, `grep -vE ""` matches every line, `-v` emits nothing, and every foreign path reports clean — reproduced: `printf 'contracts/x\n' \| grep -vE "" \| grep -c .` → `0`. Defeats RB-03, RB-04:347, RB-05, RB-07, RB-11 and RB-13 simultaneously. | `set -euo pipefail` plus a literal re-assertion of `L1_ALLOW` (and `$CP`, `$TASK_ID`, `$BR`, `$PR`) at the top of every block, with `[ -n "$L1_ALLOW" ] \|\| exit 1`. |
| **B33** | blocker | L1-07 | 350, 579, 654, 712, 1013; RB-04:346-347, 398 | command-error | (a) `git add -- schemas/registry schemas/product registries validators/registry 2>/dev/null` is all-or-nothing: if any tree is absent git exits 128 and stages **nothing**, with stderr suppressed. RB-13.1 then prints `NOTHING TO PARK` and pushes an empty branch while acceptance 13-A reports PASS — work silently lost. (b) RB-04's two "refusals" only `echo`; they sit in the same block as `git commit`/`git push`, so an agent told it was refused pushes a branch carrying foreign paths anyway. | Loop `for d in …; do [ -e "$d" ] && git add -- "$d"; done` then assert something is staged; never suppress git stderr. Make RB-04's refusals terminal (`… \|\| { echo 'REFUSE: …'; exit 1; }`). |
| **B34** | blocker | L1-07 | 645 | path-ownership | "Do **not** widen `platform.yaml`'s `supported_contract_versions` — that file is not yours." `registries/**` is L1's exclusive tree per PARTITION, and L1-01-10 authors it. Telling the agent its own registry is foreign will cause a spurious escalation or, worse, distrust of the allowlist the whole document depends on. | Restate as the content rule it actually is: the file is L1's, but the version floor is consumed by L3/L5 and widening it is a D93 fleet-blast-radius change → RB-11. |
| **B35** | blocker | L1-99 | D1:39-49; 22, 25, 470, 499-501, 530 | citation / completeness | **L1-99 is stale and is being read as a work queue.** Its first BLOCKING defect ("30 of 61 task bodies missing") is false (SC-4); its headline "citation layer is sound, 25/25 anchors resolve" is false (SC-5); its "127/127 control coverage" denominator omits the 30 bodies it thought absent (real total 157). It carries no "reference only" banner, sits beside eight dispatchable documents, and ends with a ten-row imperative work table with no ids, owners, branches, SELF-VERIFY or STOP rules — one row of which orders work already done. | Add a first-line banner: "REVIEW ARTIFACT. Not a dispatch document." Delete/rewrite D1, downgrade check 6 to FAIL, recount the verdict banner (8/7/**5**/4 = 24 numbered defects, not 25), and add the missing MINIMUM SET rows for D11, D13, D17. |
| **B36** | blocker | L1-00 | 489, 495, 524 | command-error | The lane guard and T02's A3 derive changed paths from bare `git status --porcelain`, which **collapses untracked directories**: after T02 creates the four trees it emits only `?? registries/`, `?? schemas/`, `?? validators/`. `schemas/` and `validators/` fail the ALLOW regex, A3 returns `2`, the STOP fires, the agent runs `git checkout -- .` and files a FOREIGN-PATH blocker on a correct tree. The lane halts at task 2 of 5. | `git status --porcelain --untracked-files=all` at all three sites. |
| **B37** | blocker | L1-00 | 547; 524-525 | path-ownership / command-error | (a) T03 criterion A3 proves the guard by `touch docs/__probe.tmp` — `docs/**` is L0's and is in this charter's own "do not touch" table; and if `docs/` is absent the `&&` short-circuits so the guard never runs while `exit=1` still prints. (b) `lane_paths.sh` swallows an unresolvable `$BASE` with `2>/dev/null \|\| true`, so the guard reports LANE-GUARD-PASS on a branch it never examined — and T05 invokes it with `origin/integration`, which T01 never verifies exists. | Probe a repo-root scratch file (`__lane_probe.tmp`) and assert the literal FAIL line, not the exit code alone. Add `git rev-parse --verify "$BASE" \|\| { echo "LANE-GUARD-FAIL: base ref does not resolve"; exit 1; }` and drop the `\|\| true`. |
| **B38** | blocker | L1-01 | T07:887-978, 1602-1604 | citation / command-error | §52.6 is transcribed from the wrong lines and counted with a non-terminating awk. The real table body is spec 4624-4653 (30 rows); the file uses 4619-4647 (29) with header rows 4617-4618, truncating six rows. The row-count awk `/^\| --- \| --- \| --- \|$/{f=1;next} f&&/^\|/{c++}` never resets on the following 2-column `## Provenance` table, so it over-counts by 6 and can never equal the expected value — failing T07, `registry-inventory-check`, `registry-verify`, T12 and T13. The committed "transcription note" also states a false fact (spec 4644 is the `records/`+`events/` row, not conduct-records + `templates/`). | Re-range to 4624-4653 and 4622-4623; change 29→30 everywhere including `registry.mk`; add `f&&!/^\|/{exit}` to both counting invocations (the FILE_SHA awk already has it); delete the transcription note. |
| **B39** | blocker | L1-01 | T10:1260-1261, 1306, 1314, 1393; 1308-1319 | contradiction | (a) T10 hard-blocks on two frozen inputs no document produces: `contracts/event-types.txt` and a canary-set artefact. L0-01 writes the enum to `contracts/records/event-type.enum.v1.yaml`; no document anywhere produces a canary set. T10 STOPs permanently and T13 (depends on T10) never completes. (b) The seeded `platform.yaml` emits `event_types` as plain scalars plus a top-level `retired_event_types: []`, contradicting L1-02:1383 (`{id, retired}` objects, `additionalProperties: false`) and L0's PLAT-R1 ("a retired identifier is marked `retired: true` and is never removed"). The file T10 commits fails the validator L1-02 builds against it. | Point T10 at `contracts/records/event-type.enum.v1.yaml` with a YAML transform (not `sed 's/^/  - /'`); name the real canary-set artefact or raise a DECISION REQUIRED. Emit `event_types` as `{id, retired: false}` mappings, delete `retired_event_types`, update criteria 4/6/7/8. |

*(B1–B39 with gaps: numbering follows the merge order; 30 distinct blocking families remain after B4, B5, B7, B13, B15, B33, B37, B38, B39 absorbed 36 duplicate findings.)*

---

## 3. MAJOR DEFECTS (71 — grouped by fix family)

| Family | Files | Summary | Fix class |
|---|---|---|---|
| M-A | L1-01, L1-02, L1-03, L1-04, L1-06 | **Blocker-template and label defects.** `gh issue create --label blocker,lane-N,phase-N` fails outright when a label does not exist, and no task creates them; L1-02 §4 and L1-01 §0.6 wrap the template in a ```` ```bash ```` fence containing bare ``` fences, so a copied block is truncated mid-heredoc. Every STOP in five documents routes here. | mechanical |
| M-B | L1-02, L1-03, L1-04, L1-05, L1-06 | **No `set -euo pipefail` anywhere; validation never gates git.** L1-02 T01/T02/T04/T05/T19 push and open PRs after unvalidated (or exit-2) runs. | mechanical |
| M-C | L1-02, L1-04, L1-05, L1-06 | **`grep -c` misuse.** `grep -c` exits 1 on a zero count, so the case the criterion calls success also reports failure; with two file arguments it prints two `path:count` lines; `grep -rc` over a directory prints per-file lines. ~12 acceptance criteria are unsatisfiable as written. | mechanical |
| M-D | L1-02, L1-04, L1-05, L1-06, L1-07 | **Interpreter, `/tmp` and toolchain portability on Git Bash / Windows 11.** L1-04 uses `python3` exclusively (40 sites) where siblings use `python` (383 sites) and Windows resolves `python3` to a Store stub; seven L1-04 commands hand Git-Bash `/tmp` paths to a native Python that resolves them as `C:\tmp`; L1-05-L1-904 times the suite with `/usr/bin/time`, absent from Git for Windows, so the runtime budget is never checked; no task runs `pip install` or creates a venv, and L1-03-00 imports undeclared PyYAML. | mechanical + one L0 toolchain decision |
| M-E | L1-02, L1-03, L1-05, L1-06 | **Wrong arithmetic in acceptance criteria.** L1-02-02 c1 expects `2`, actual `4`; L1-05-L1-601 `REQ=19` vs 18 fields and 20 vs 21 cases; L1-05-L1-603 `REQ=21` vs 20 and 16 vs 17; L1-05 §2 totals `S=14/M=26/L=21` vs actual `11/31/19`; L1-05 §5 "THIRTEEN blockers" over a 14-row table; L1-704 expects `12 file(s)` where 13 exist; L1-05 master rows 15 and 28 contradict their own bodies (`11 passed` vs `10 passed, 1 xfailed`); L1-D02 says 24 capability tokens where L1-101 correctly says 27; L1-06-06 says 17 pairs then 19; L1-06-10 hard-codes `min_valid=38`/`min_invalid=50` that T12 invalidates. Under the lane's character-for-character rule each sends the executor into a retry-then-blocker loop. | mechanical |
| M-F | L1-02, L1-03, L1-04, L1-05, L1-06 | **Self-verifications that cannot fail or can be faked.** Four L1-03 SELF-VERIFYs run only the test module the same agent wrote; L1-04-T-09 step 4 diffs the empty range `origin/integration~0..HEAD`; L1-04's canary rule returns a finding in every branch so `canary_hits==0` is unreachable and Layer 2 is never exercised; L1-02-04/T06/T16 `&&`-chain so a failing lint prints the same `EXIT=1` as success; L1-05 §3 proof 1 uses `gh pr list --search "lane/1/"` which matches nothing whether or not PRs are open; L1-06's `LANE1 SUITE: PASS` prints in modes that run zero canaries. | mechanical |
| M-G | L1-02, L1-03, L1-05, L1-06, L1-07 | **Judgment leakage.** L1-03-08 (8 undesigned YAML deltas), T11 (12-row policies.yaml + free-text `graded_detail`), T15; L1-02-08 requires coining two event-type identifiers its own DECISION-L1-02-B forbids; L1-06-05 "choose the single longest line that names the rule and freeze it" ×50; L1-06-09 "delete exactly one required field" with no field named for any of 18 schemas; L1-07 RB-09 free-form YAML merge and RB-11 "enumerate the options"; L1-05-L1-505 silently resolves "Founder or named delegate" to `founder`; L1-05-L1-803 prose fixture delta. | **L0 decides** |
| M-H | L1-02, L1-03, L1-04, L1-06 | **Missing path-ownership controls and shared-tree collisions.** Twelve of nineteen L1-02 tasks carry no acceptance table and no runnable foreign-path check; L1-03 §4 stages three whole trees (`git add validators/registry schemas registries`) then asserts nothing outside `validators/registry/` is staged; L1-04-T-06 `cp -a schemas` copies L4's `schemas/records/**` into an L1 fixture; L1-06 writes into `validators/registry/tests/`, already claimed by L1-03 and L1-05. | mixed |
| M-I | L1-03 | **Unbuilt fixtures and untested finding codes.** Eight tasks create 18 fixture directories whose deltas and `expected.json` are never written, leaving 12 declared codes (R02.3, R02.4, R04.2, R04.3, R06.1, R06.5, R09.2, R09.4, R09.5, R09.6, R15.4, R15.5) exercised by no fixture — so T20's closure assertion 3 fails the whole suite after eighteen branches have merged. The content of a `test_r<NN>_*.py` module is never specified in 2,029 lines while every criterion 1 asserts a hard-coded pass count. | mechanical after B13/B14 |
| M-J | L1-02, L1-03, L1-04, L1-05, L1-07 | **Contradictory conventions between siblings.** Branch grammar (`lane/1/p3-L1-03` vs `lane/1/l1-003` vs `lane/1/00-charter`); commit subject (`L1 <id>: …` vs `<id>: …`) — L1-07's own 04-B and 13-C fail on every commit the task documents produce; §0.1's `v<N>` rule vs `platform/v1`; L1-02-19's `schemas/registry/index.json` is a registry-of-everything banned by PARTITION rule 3 and refused by L1-05-L1-704; `check-commitment` is deferred by two documents to a task that exists in none, while L1-00:50 says L1 builds it and L1-05:1706 says it is out of scope. | **L0 decides** |
| M-K | L1-01, L1-02, L1-05 | **Field-shape contradictions inside one lane.** `investment_gate_answers` has three shapes (L1-02-13 object q1..q7; L1-02-14 seven named keys; L1-05:4185 array minItems 7) all under a ratification claiming to fix names literally. `operating_system_entry` is flat in L1-05 and per-phase in L1-02; §57.2 requires per-phase plus a ritual inventory and a cash line. `platform_version` is `string` in L1-02, `["number","string"]` in L1-01-06, and unquoted `4.0` in L1-01's seed and in the spec block L1-02-08 says to transcribe verbatim. `automation_ledger[].failure_cost` is an object in L1-02 and a string in L1-05. `supported_contract_versions.product` is `[1]` in L1-01-10 against §60.1's `[1, 2]`. | **L0 decides** |
| M-L | L1-00, L1-02, L1-05, L1-06 | **Spec unreachable or mis-pathed.** L1-02 gives three mutually inconsistent spec paths including the literal `/path/to/`; L1-05-L1-101 resolves the spec to `$HOME/work/MultiProduct/Research/…` and always prints its fallback while three STOPs require re-reading the spec; L1-06 never names the spec at all; L1-00 hard-codes an absolute Windows path into a committed shell script. | mechanical |
| M-M | L1-00, L1-02, L1-03, L1-04, L1-06, L1-99 | **Wrong or unsupported citations (individually verified).** L1-00's §99.2 range repeated 12×, six further wrong section ranges, §21.4 cited two different wrong ways; L1-02-13 "§101.11 invariant 90" (it is §101.12) and "three factors omit `scale`" (five do); L1-03 R11 cites invariant 11 (which says something else) and caps D at 112; L1-04 cites §96.4 for a phase list it does not contain and implements only half of §53.1 (no per-registry comparison counts); L1-06 tags a residency fixture AT-105 (which is about deletion SLAs) and cites §54.2 for an exception-owner rule it does not state; L1-99 cites invariant 80 for a fail-closed rule it does not state and gives §100 as 9295-9442. | mechanical |
| M-N | L1-00, L1-03, L1-04, L1-06 | **Interface/ownership items with no owner.** L1-00 DoD-9 requires validating per-product `product.yaml` files L1 does not own; DoD-11 is "provable by nothing"; §5's five rows say "file a CCR" and §13 defines no CCR kind; L1-03-20 c5 runs `--root .` where `products/**` and `shared-services/**` match nothing so the whole 18-rule suite passes vacuously; L1-03-10 defers renewal counting to Subsystem O, which PARTITION assigns to no lane; L1-04 D-1 asks for one required-check context where §33.2 and L2-05 require two (`contract-validation`, `registry-validation`); L1-04 D-2 hands L2 an invocation contract informally rather than through `contracts/**`; L1-06 §2 leaves two §52.6 rows (Leave records, Performance framework registry) with no disposition. | **L0 decides** |

**File-local majors outside these families:** 9 in L1-02, 6 in L1-03, 5 in L1-04, 4 in L1-05, 3 in L1-06, 6 in L1-07, 6 in L1-99, 9 across L1-00/L1-01 (round 1) — all recorded in the round-2 findings and `review-harvest/_ALL_DEFECTS.json`.

## 4. MINOR DEFECTS (44)

Dead variables (`C=` in L1-02-04, `$defs.C` in people.schema.json), off-by-one prose counts (L1-02-17 "twelve primitives" from nine bullets; L1-03-18 "ten entries" over nine; L1-05-L1-307 duplicate boundary fixtures), unanchored repo guards (`grep -q 'control-plane'` also matches `control-plane-records`, L4's repo), Windows `test -x` on a checked-out mode bit, `/tmp` collisions and unrestored fixtures on a failed `&&` chain, whitespace mismatches between a script's `echo` and its "correct output" block, missing package `__init__.py` files, an invented currency token in a Founder-owned registry, procedure-manifest rows contradicting their own procedure headers, and acceptance rows whose command differs from the SELF-VERIFY line for the same criterion id. None blocks execution alone; several will produce spurious blocker filings under the lane's character-for-character comparison rule.

---

## 5. LANE VERDICT

# BLOCKED

Lane 1 cannot be dispatched. The reasoning, in order of force:

**1. The lane has no single interface with itself.** Five schema-path conventions (SC-1) and four "frozen, only interface" validator contracts (SC-2) coexist across seven documents. Three documents — L1-03, L1-04, L1-06 — are fail-closed on artefacts that another document's convention makes impossible, so each halts at its own task 0/1/2. This is not a set of bugs; it is the absence of a lane architecture. B1 and B2 must be settled by L0 before any editorial fix has a stable target: every path, import, `cp`, `find` filter and acceptance command in the lane is downstream of them.

**2. The lane's sole runbook entrypoint does not exist** (SC-3, B3). Ten of fourteen procedures and the fail-closed preflight depend on a file no task creates.

**3. Every executor is guaranteed to stall on its first command.** No task creates or clones the repository, four different root variables are used and three are never set (B7); `cd ""` fails silently. L1-00's guard rejects a correct tree at task 2 of 5 (B36). L1-02's harness fails its own two literal schemas and then pushes a PR anyway (B9, B10).

**4. The lane's own verification is not trustworthy.** The mutation harness cannot detect a mutation (B29), the layerability negative cannot fail (B30), the canary layer is never exercised (M-F), the lane guard fails open when a variable is unset (B32), and the end-to-end `--root .` proof passes vacuously (M-N). A lane whose instruments cannot fail cannot certify itself — which is the discipline three of these documents cite as their own thesis.

**5. The lane is written against a superseded spec.** 20 of 25 pinned anchors are wrong (SC-5, B6), and the pre-amendment counts (46 signals, 29 artifacts, D1-112, SIG-43) are not merely cited but **compiled into shipped artefacts and into unconditional STOPs the executor cannot satisfy** (B5).

**6. The lane's own review says it is closer to ready than it is.** L1-99's headline blocker is refuted (SC-4) and its headline PASS is refuted (SC-5); it carries no "reference only" banner and ends with an imperative work table, one row of which orders work already done. It must not be used as the dispatch queue (B35).

Round 1 reviewed only L1-00 and L1-01 and found 10 blockers there; round 2 found 56 more across L1-02..L1-07 and L1-99. After dedup: **30 blocking families, 71 major, 44 minor.** Of the 30 blockers, **11 require an L0 decision** and cannot be closed by editing L1 at all.

---

## 6. SHORTEST ORDERED FIX LIST TO REACH READY

### Stage 0 — L0 decisions (nothing in L1 can be usefully edited before these land)

| # | Decision | Closes | Effort |
|---|---|---|---|
| D-1 | **One schema path/naming contract** under `contracts/**`: path grammar, `$id` host, `_common` vs `common`, and the disposition of L1-01's two shared-defs files. | B1, B11, part of B16 | judgment |
| D-2 | **One validator contract**: CLI grammar + exit codes + output grammar; one rule-module shape; one rule-id grammar; one `requirements.txt`; one lane entrypoint path and verdict line. Mark the losing documents superseded. | B2, B3, B4, B21, M-J | judgment |
| D-3 | **Repository bootstrap fact** (repo URL, org login, clone path, one root variable name) **and the branch/merge model** for a lane whose tasks consume each other's output. | B7, B8 | judgment |
| D-4 | **DECISION-L1-02-A** (product v1 shape) and **DECISION-L1-02-B** (event-type identifiers): resolve, or formally defer with a stated fallback. | B16, M-G | judgment |
| D-5 | **Ownership assignments**: `registries/policies.yaml`; the `validators/registry/tests/` sub-tree; the AT-102 canary (L1 vs L3); `validators/registry/README.md` (three authors); `products/**` and `shared-services/**`; Subsystem O; `check-commitment`; `verification/contract.yaml`. | B17, B20, M-H, M-N | judgment |
| D-6 | **Severity contract**: does Red block a merge? (§53.4 says no; L1-04 codes yes.) And: is an intra-lane generated `index.json` exempt from PARTITION rule 3? | B19, M-J | judgment |
| D-7 | **Field shapes**: `investment_gate_answers`, `operating_system_entry`, `platform_version` type + quoting, `automation_ledger[].failure_cost`, `supported_contract_versions.product`. | M-K | judgment |
| D-8 | **Authoring split** for L1-02-05..T18 and L1-06-03/T04/T06: L0 authors the literal artefacts, or L1 transcribes frozen `contracts/**`. | B12, B31, M-G | judgment |
| D-9 | **Toolchain**: interpreter name, Python version, venv/provisioning step, temp-directory convention for Git Bash on Windows. | M-D | judgment (small) |

### Stage 1 — mechanical sweeps (scriptable; S-1, S-2 and S-5 can start immediately)

| # | Sweep | Closes |
|---|---|---|
| S-1 | **Spec-count sweep**: 46→47, `SIG-01..SIG-46`→`..SIG-47`, SIG-43→SIG-47 at L1-02:1659, 29/twenty-nine→30/thirty, D1-112→D1-113, "10,214 lines"→10,231. Then delete the three now-unsatisfiable unconditional STOPs (L1-05-L1-505/L1-605/L1-902), the L1-704 known-red framing and the L1-904 R-INV-01 canary. | B5 |
| S-2 | **Anchor re-pin**: all 25 rows to measured values; record the spec SHA-256; correct the ~20 inline range citations in L1-00/L1-01/L1-99; re-range L1-01-07 to 4624-4653. | B6, B38 (part), M-M |
| S-3 | **Shell hygiene**: `set -euo pipefail` plus a literal re-assertion of every variable at the top of every command block; `--untracked-files=all`; `git add` loop instead of multi-path pathspec; terminal `exit 1` on every refusal; `git rev-parse --verify "$BASE"`; drop `2>/dev/null \|\| true` from guards; `\|\| true` after every `grep -c`; `-h`/`-l` where multiple files are passed; fix the four allowlist and repo-name regex anchors; replace `origin/main...HEAD` with `origin/integration...HEAD`. | B32, B33, B36, B37, B27, M-B, M-C |
| S-4 | **Harness fixes** (small, local, verified): L1-02 `lint()` applicator skip-list; `makeAjv(skipPath)`; L1-03's three `cat >>`→python splices and the T01 exit-2 idiom; L1-06 `mutate.sh` (delete the no-op loop, iterate the valid corpus, add `{SCHEMA_ROOT}`); L1-06 T13 half-2; L1-01's two counting awks; L1-04 `BLOCKING_SEVERITIES`, the `tests\|tools` skip-list and the dry-run gate. | B9, B10, B15, B19, B29, B30, B38, B4(a,c) |
| S-5 | **Arithmetic sweep**: recount every hard-coded count against its own table (23→24 AT rows; `REQ=19`→18; `REQ=21`→20; 20→21 and 16→17 cases; S/M/L totals; THIRTEEN→FOURTEEN; 12→13 files; 24→27 tokens; master rows 15 and 28; L1-02-02 c1 2→4; L1-06 min_valid/min_invalid), then re-derive the dependent lane totals (`698 passed`, L1-06's 174-file and 107-fixture counts). | B28, M-E |
| S-6 | **Metadata sweep**: extend the nine L1-05 FILES fields; add the missing `git checkout -b`/PR blocks to L1-06 T07/T08/T12/T15; add acceptance tables and the runnable foreign-path check to L1-02 T07-T18; create the `blocker`/`lane-N`/`phase-N` labels in a Phase-0 task; fix the two truncated ```` ```bash ```` blocker templates; add the literal spec path to L1-02/L1-05/L1-06; add the missing `__init__.py`; unify branch and commit-message grammar per D-3. | B23, M-A, M-H, M-J, M-L, most minors |

### Stage 2 — re-authoring that only Stage 0 makes possible

| # | Work | Closes |
|---|---|---|
| R-1 | Rewrite L1-03-00/T02, L1-06 §2/T01/T02, L1-02-19 and L1-05's schema tasks against D-1; rewrite L1-04 §1.3/§1.5/`gate.py` and L1-07 RB-02..RB-13 against D-2; add the `rule.yaml` generator or its CCR. | B1, B2, B3, B4 |
| R-2 | Fix the L1-03 baseline: publish the kind→key map, add `waiver_decision_record`, scope R06.3, and choose the per-fixture rule-scoping rule — **all inside T01/T02 before T03+ are dispatched.** Then build the 18 unbuilt fixtures and their `expected.json`, and specify the test-module template. | B13, B14, M-I |
| R-3 | Supply literal artefact content per D-8 for L1-02-05..T18 and L1-06-03/T04/T06; build L1-03-15's `_base/` trees; build L1-06-09's canary deletions. | B12, B18, B31 |
| R-4 | Fix L1-05's fixture-regeneration chain (B24), the roles.yaml fixture copies and L1-904 check 4 (B25), the L1-310/L1-404 ordering (B26), and L1-005's fixture-kind contract (B22). | B22, B24, B25, B26 |
| R-5 | Correct L1-07 RB-04's refusal semantics and the `platform.yaml` ownership statement; add RB-02's anti-fabrication controls; add the merged-state guard to RB-13.2 before the destructive branch delete. | B34, B3, majors |
| R-6 | Re-issue L1-99 as a marked review artefact with corrected counts, or retire it in favour of this document. | B35 |

### Mechanical vs judgment split

- **Judgment (L0):** 9 decisions closing **11 blocking families** and ~22 majors. This is the critical path; nothing else finishes first.
- **Mechanical (sweeps S-1..S-6):** close **12 blocking families**, ~35 majors and most of the 44 minors. Roughly **60% of the total defect count is a scripted sweep**, but only ~40% of the *blocking* count — the blockers are disproportionately architectural.
- **Re-authoring (R-1..R-6):** the remaining **7 blocking families**. Substantial: L1-02's 14 content-free tasks and L1-06's 76 content-free files are the largest single body of work in the lane and are gated on D-8.

**Sequence:** D-1 → D-2 → D-3 (answer together; they unblock everything else) → D-4..D-9 in parallel → S-1, S-2, S-5 (independent, start now) → S-3, S-4 → R-1 → R-2 → R-3..R-6 → S-6 final pass → re-review.

**Do not dispatch any L1 task until D-1, D-2, D-3 and S-1/S-2 are closed.** Every task in the lane either reads a schema path, calls a validator, or `cd`s to a repo root — and all three are currently undefined or contradictory.
