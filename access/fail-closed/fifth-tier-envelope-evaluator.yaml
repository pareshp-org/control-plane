# access/fail-closed/fifth-tier-envelope-evaluator.yaml
# Section 64.2 lines 5448-5468; invariant 80: every control is explicitly
# classified fail-closed or fail-open. One file per control (PARTITION rule 3).
schema_version: 1
control_id: fifth-tier-envelope-evaluator
title: "A run outside the behavioural envelope is Blocking"
classification: fail-closed
implemented_by: "access/secrets/envelope/check-envelope.sh"
closed_means: "an unreadable envelope, an unreadable run record or an empty run directory reports FAIL, never PASS"
on_input_unreadable: fail
on_check_error: fail
spec_anchor: "Section 40.1 line 3685, D96"
phase: L5-P2
