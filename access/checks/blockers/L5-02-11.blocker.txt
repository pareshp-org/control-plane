BLOCKER L5-02-11: access/secrets/separation/separation.result.yaml cannot be built for real - the frozen input contract is absent
task_id:                L5-02-11
lane:                   L5
plan_file:              Code/implementation/lanes/L5-02-secrets-and-boundaries.md
trigger:                access/secrets/boundaries/check-boundaries.sh --phase complete reports ASSERTION B2-A5 FAIL because access/secrets/separation/separation.result.yaml is absent, and a dependent check raises FileNotFoundError reading it. Per the L5-02-11 spec, that file is not hand-authored - it is generated by `python access/secrets/separation/check_separation.py --write`, run against contracts/access/access-inputs.yaml (default input), the L0-frozen input named in "Frozen inputs" (L5-02 lane file, lines 45-114).
command_run:            python access/secrets/separation/check_separation.py --write
observed_output:        RESULT FAIL inputs-unreadable contracts\access\access-inputs.yaml (exit 2)
expected_output:        SEPARATION separated (or SEPARATION accepted-risk holder=... review=...) / WROTE access/secrets/separation/separation.result.yaml / RESULT PASS (exit 0)
blocked_because:        contracts/access/access-inputs.yaml does not exist anywhere in this repository - confirmed absent on every local branch (master and all worktree-* branches) and in the full commit history (git log --all -- contracts/access/access-inputs.yaml returns nothing). Only test fixtures under access/secrets/testdata/**/access-inputs.yaml exist; none is the real contract, and check_separation.py fails closed (exit 2) on a missing input by design. The lane file is explicit that this file is L0-owned and frozen at Phase 0 (PARTITION.md rule 2; FD-013 per _FOUNDER_DECISIONS.md FD-079): "The executor never edits it and never creates it." Determining separation.result.yaml's content requires knowing, for real, which people.yaml id holds fifth-tier rotator duties for each of the five machine credentials and which person holds ops-vm host root (contracts/access/access-inputs.yaml fifth_tier.*.rotator and ops_vm.host_root_holder) - a real org/identity fact and, per the script's own docstring, "a decision for L0, never for the executor." Hand-writing separation.result.yaml directly, or inventing plausible values for the missing contract just to drive the real script, would both produce a fabricated result (a false "separated"/"accepted-risk" claim about real credential custody) rather than the genuine one the task requires.
expected_output_note:   access/secrets/separation/check_separation.py is implemented correctly and fails closed (exit 2) exactly as designed when its input is missing; no code change is needed there.
requires_decision_from: L0 integrator (the Founder) - contracts/access/access-inputs.yaml must be authored with real values (and, if any fifth-tier rotator is also the ops-vm host-root holder, a real accepted-risk record must be authored at contracts/access/accepted-risks/rotator-hostroot.yaml with all 14 REQUIRED_RECORD_FIELDS). After that, `python access/secrets/separation/check_separation.py --write` will produce a genuine separation.result.yaml and `bash access/secrets/boundaries/check-boundaries.sh --phase complete` will pass ASSERTION B2-A5.
