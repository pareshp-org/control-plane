# access\secrets\envelope/organisation-export-token.envelope.yaml
# Generated by access/secrets/tools/emit_envelopes.py.
# Do not hand-edit: re-run the emitter.
# Section 40.1 line 3685; D96 line 10189; Section 53.4.
schema_version: 1
credential_id: organisation-export-token
spec_anchor: "Section 40.1"
spec_lines: "3685"
decision: D96
temporal_window_rule: retired
temporal_window_rule_reason: "Use outside its scheduled window is near-vacuous for a credential that legitimately fires hourly, nightly and on every registry merge, and is silent entirely for read-side abuse (D96)"
signed_run_record:
  required: true
  must_name: scheduled_trigger
  schema: "access/secrets/schemas/run-record.schema.json"
  unmatched_run_class: Blocking
  rationale: "any run without a matching trigger record raises immediately"
run_count_ceiling_per_day:
  scheduled_runs_per_day: 1
  multiplier: 2
  value: 2
  computed: true
  status: "calibrated configuration; initial value twice the scheduled run count"
  recalibrate_when: "the schedule changes"
  breach_class: Blocking
expected_source_host:
  value: "ops-vm-control-plane-01"
  breach_class: Blocking
  rationale: "the credential is expected to be used from the host that holds it (Section 40.3 line 3703), so the envelope is host-aware and not only time-aware"
published_api_call_counts:
  required: true
  per: run
  absent_class: Blocking
  rationale: "read-side abuse - enumerating and cloning every private repository - emits no webhook and no event-log entry, so it must surface as a volume anomaly rather than as nothing"
objects_mutated_cap: not-in-envelope-v1
objects_mutated_cap_escalation: "E-P2-01 - D96 names per-run caps on objects mutated; the Section 40.1 line 3685 envelope list does not, and the frozen contract carries no key for it. L0 decides."
outside_envelope_class: Blocking
severity_scale: "Section 53.4 lines 4704-4716 - the only severity scale in the system"
alert_config_owner: "bendrohit-eng"
alert_config_owner_is_named_control: true
alert_config_owner_rationale: "so the alert on a run outside the envelope is itself an owned control, not an unowned hope (Section 40.1 line 3683)"
evaluator: "access/secrets/envelope/check-envelope.sh"
