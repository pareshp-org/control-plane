# HUMAN ACTION REQUIRED — L5-07-05 / NC-05

**Executor:** organisation Owner
**Why a human:** the step requires the machine account's own credential submitting a live review against a real PR whose CODEOWNERS path applies; no AI executor may hold or exercise a machine credential this way
**Spec clause:** Section 98.2 Phase 1; Section 11.3 — "an approval from a machine account does not satisfy branch protection — CODEOWNERS is generated to contain human identities only, and the check is executed negatively"
**Expected result:** merge stays blocked; the required Code Owner review is unsatisfied. The static half (the generator's refusal to emit CODEOWNERS naming a machine identity) is verified automatically by access/tests/phase1/nc-05-machine-approval.sh.

## Steps
1. Have the machine account submit an approving review on a PR whose CODEOWNERS path applies.

## Record the result
Run:
    bash access/tests/lib/record-evidence.sh NC-05 pass "<observed>"
Then commit the produced file under access/tests/evidence/ on branch lane/5/07-evidence.
