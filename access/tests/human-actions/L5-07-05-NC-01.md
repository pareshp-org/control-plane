# HUMAN ACTION REQUIRED — L5-07-05 / NC-01

**Executor:** organisation Owner
**Why a human:** the step requires a human account holding Write attempting a direct push against an armed protected branch; no AI executor may hold or exercise such a credential
**Spec clause:** Section 98.2 Phase 1 — "no direct human push to any default branch succeeds on the control-plane repository"
**Expected result:** the push is refused by branch protection.

## Steps
1. On the control-plane repository, run `git push origin HEAD:main` from a human account holding Write.

## Record the result
Run:
    bash access/tests/lib/record-evidence.sh NC-01 pass "<observed>"
Then commit the produced file under access/tests/evidence/ on branch lane/5/07-evidence.
