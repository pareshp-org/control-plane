# HUMAN ACTION REQUIRED — L5-07-05 / NC-06

**Executor:** organisation Owner
**Why a human:** the step requires attempting a push with the records-writer credential against the real, live control-plane repository; no AI executor may hold or exercise that credential
**Spec clause:** Section 40.1 (D89); PARTITION — "NO machine bypass actor (D89)"
**Expected result:** the push is rejected, and the records-writer credential must not be installed on the control-plane repository at all. The static half (no ruleset declares a bypass actor) is verified automatically by access/tests/phase1/nc-06-no-bypass-actor.sh once a repository slug and API access are available.

## Steps
1. Attempt a push to the control-plane default branch using the records-writer credential.

## Record the result
Run:
    bash access/tests/lib/record-evidence.sh NC-06 pass "<observed>"
Then commit the produced file under access/tests/evidence/ on branch lane/5/07-evidence.
