# HUMAN ACTION REQUIRED — L5-07-05 / NC-11

**Executor:** organisation Owner
**Why a human:** the step pushes a workflow declaring `environment: production` to a non-default branch and runs it against the live organisation; no AI executor may push or run a workflow this way
**Spec clause:** Section 98.2 Phase 1; Section 33.4 deployment branch policy — "a workflow pushed to a non-default branch declaring environment: production obtains no environment secret, executed negatively"
**Expected result:** the run obtains no environment secret.

## Steps
1. Push a workflow declaring `environment: production` to a non-default branch and run it.

## Record the result
Run:
    bash access/tests/lib/record-evidence.sh NC-11 pass "<observed>"
Then commit the produced file under access/tests/evidence/ on branch lane/5/07-evidence.
