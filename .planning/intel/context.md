# Context (DOC intel)

Synthesized from classified DOC documents, grouped by topic. Each note is appended verbatim in substance with source attribution, per the DOC extraction contract.

---

## Topic: Phase 0 / Bootstrap

- source: docs/bootstrap-runbook.md — Pre-Phase 0 checklist (dated 2026-09-09): all 112 Founder Decisions recorded (FD-001..112); 31/32 PFDs resolved (PFD-006 deferred); all 6 coherence reviews PASS (L0-L5-99); `verify_handover.sh` HANDOVER INTACT; `_concordance-check.sh` 5/5 lanes OK; 12 JSON schemas created; registries initialized with bootstrap seeds; validator CLI all 18 rules (R01-R18) implemented; 41 integration tests passing; 16 CI/CD workflow files; `run-phase-0.sh` verified end-to-end via `--dry-run` with zero credentials required. Three former blockers are now self-handled by `run-phase-0.sh` on every run: the `main`/`integration` branches auto-bootstrap with an empty initial commit if missing; the 89-entry `event_type` enum is self-authored (D114-D); all 23 registered contract stubs are auto-created by `append_register.py`. Remaining before B-Day / Phase 1: Phase 0 execution itself (needs `GITHUB_TOKEN`), 8 product names for `facts.tsv`, the `PFD-006` break-glass second-owner decision, and 18 protocol blocking defects (`protocol/_98-DEEP-REVIEW.md` §4). Break-glass procedure documented (4-step) with second owner still TBD.

- source: docs/phase-0-complete.md — Contracts frozen at tag `contracts/v1.0.0`, hash `07232bd31808ca03c289e69cb7b660d4b67bbdb253f7bc13cca04e0f67eb631b`, dated 2026-09-16 (later than the bootstrap-runbook snapshot above). Twenty-four contracts exist under `contracts/**`, each with a seven-key header, a register row, a stub, and at least one golden-valid and one golden-invalid fixture. `make contracts-verify` proves this in one command; `contracts.sha256` records the freeze; `make promote-check` prints `CONTRACTS-FROZEN OK` while it holds. Code Owner review is required on `contracts/**`, and the freeze tag is immutable with no bypass actor. Five gaps recorded, dated and owned in `docs/recorded-gaps.md`. Binding rule: no lane edits `contracts/**` — a lane needing a change files a Contract Change Request (`docs/contract-change-request.md`) and stops.

- source: lanes/L0-CONCORDANCE.md — Session 13 (2026-09-08) log: `protocol/00-dispatch-model.md` created, publishing the canonical Q1–Q12 dispatch-gate question sequence; FD-071..081 written, resolving all dispatch-gate questions (81 FDs closed at that point); `run-phase-0.sh` created (1,060 lines); `PENDING_FOUNDER_DECISIONS.md` created and grown from 19 to 32 PFDs over the session; `L1-CONCORDANCE` completed with 0 orphans; L5 orphan indexing completed (26 bodies); L3 commands backfill completed (73 blocks); the L0-04 register closed 7 REG items; `verify_handover.sh` reported HANDOVER INTACT with 0 failures/warnings; Phase 0 status recorded **COMPLETE** — all 26 Phase 0 task bodies present and executable across all lanes, pending only the GitHub Team-tier purchase by the Founder before remote execution can proceed.

---

## Topic: Lane 5 Integration Readiness

- source: infra/READINESS.md — 15 of 16 readiness gates PASS (each executable via `bash infra/tools/readiness.sh`, no live host/network/API call required). The one open gate is `asset-inventory`: `assets/validate_assets.py` reports `FAIL (27 errors)` — documented finding B-13 (`lanes/L5-98-DEEP-REVIEW.md`): three inventory entries (`layer-b-backup-credential.yaml`, `layer-b-backup-encryption-key.yaml`, `layer-b-selfview-pubkey-registry.yaml`) were written under a different phase's field set (`owner_capability` instead of `owner`, `alert_threshold_days` instead of `alert_days`, `expiry_tracked: true` instead of a real `expiry_date`) and fail the closed Section 49.1 schema. Fix is named: L0 fixes one entry schema, one `id` key and one filename set, applied to L5-03/L5-05/L5-06 in a single change. Five items routed to L0 as DECISION REQUIRED: (1) the private VPN/tunnel path to the ops VM and who holds it; (2) the off-VM dead-man witness location (outside VM, GitHub, and product providers, D94); (3) the background-inference host's real hardware/site/power-network-provider fields; (4) the severity a departing asset owner's orphaned certificate/domain carries when no successor is named; (5) custody of the Section 42.2 phone-escalation numbers (never committed to the repository). Several ASSISTED tasks await a human: messaging webhook creation, per-person public-key registration/annual re-key, org-settings arming, ops-VM provisioning, the AT-110 reconciler-credential live probe, and the AT-022 break-glass drill. Lane 5 explicitly claims no part of subsystems G, H, J, O or P.

---

## Topic: Architecture Overview (informal)

- source: docs/architecture.md — Informal 5-lane diagram and summary: L0 Integrator (merge authority, dispatch gate); L1 "Schema Contracts" (validator, schemas, registries); L2 "CI/CD" (branch guard, pipelines, releases); L3 "Record Store" (provisioning, records); L4 "Metrics" (scorecards, capacity, planning); L5 "Access/Governance" (policies, audit, gate). Lists selected FDs (FD-067, FD-082, FD-086, FD-094, FD-096, FD-100, FD-102, FD-106, FD-108, FD-112) and a repository-structure overview (`contracts/`, `lanes/` — 66 plan files, `schemas/` — 12 files, `registries/`, `records/`, `validators/`, `scripts/`, `.github/workflows/` — 16 files, `tests/`, `ops-vm/`, `docs/`). Phase 0 status noted as `run-phase-0.sh: READY`, repos `pareshp-org/control-plane` and `pareshp-org/control-plane-records` (public).
- **Note (see `INGEST-CONFLICTS.md` INFO-2 and INFO-3):** this document's lane labels ("L3 = Record Store", "L4 = Metrics") and its stated FD-106 canary location (`registries/people/_canary.yaml`) diverge from the higher-precedence SPEC/ADR sources in this ingest set. Treated here as an informal, superseded simplification; the authoritative values are carried in `constraints.md` (PARTITION.md / lane charters) and `decisions.md` (`_FOUNDER_DECISIONS.md`).

---

## Topic: Task-ID Concordances (L0–L5)

Each concordance is additive (per FD-004: "publish a concordance, do not reissue the indexes") — it edits no task file, authors no task body, and maps index ids to task bodies that already exist.

- source: lanes/L0-CONCORDANCE.md — L0 is the orchestration lane; its "concordance" tracks protocol files, dispatch-gate decisions and Phase 0 completion status rather than a traditional index-to-body mapping (see Phase 0 topic above for its Session 13 log).
- source: lanes/L1-CONCORDANCE.md — Status **COMPLETE**, residue **0**: all 61 index ids Lane 1 promises have a full executable body (measured across 8 files totaling 17,360 lines on 2026-09-02). Corrects an earlier review (`L1-99-review.md`) that had measured a superseded copy and wrongly claimed 30 of 61 bodies missing.
- source: lanes/L2-CONCORDANCE.md — 113 distinct task ids in the master index (`L2-05-tasks.md`), 0 residue, but 41 orphan bodies (bodies no master-index row claims), across 19,978 lines measured over 8 files.
- source: lanes/L3-CONCORDANCE.md — 82 index ids promised by `L3-06-tasks.md`, 0 residue, but 116 orphan bodies spread across seven phase files, over 26,875 lines measured across 8 files. Two competing complete implementation plans existed for this lane (`L3-06-tasks.md`'s 78+4-task plan vs. the seven phase files' 116-task plan); reconciled by FD-098 (PFD-005, Option A: `L3-06-tasks.md` authoritative, four Session-12 additions ratified).
- source: lanes/L4-CONCORDANCE.md — 117 distinct task ids promised by the index (`L4-06-tasks.md`), 107 with an actual body anywhere in the lane, across 18,896 lines measured over 8 files. Closes the defect that all six lane coherence reviews had returned BLOCKED on.
- source: lanes/L5-CONCORDANCE.md — 59 index ids promised by `L5-06-tasks.md`, 0 residue, but 93 orphan bodies, across 32,160 lines measured over 8 files (lane total 33,093 including two unmeasured review artifacts).

---

## Topic: L0 Integrator Charter (operational)

- source: lanes/L0-00-charter.md — Executor: the human lead — the one file in the entire plan whose executor is explicitly not an AI developer. Owns `contracts/**`, `CODEOWNERS`, `docs/**`, all repository root files, `Makefile`, plus every decision block in the document set. Defines the authority boundary, the seven design-open items reserved to L0 in full, the L0-only decision register (§5.1 contracts/schemas, §5.2 partition/ownership/access, §5.3 merge train/release, §5.4 programme governance), decisions lanes are absolutely forbidden to make (§6.1) and decisions forbidden without an explicit L0 ruling (§6.2). Escalation protocol: blocker-issue template and Contract Change Request template (canonical copies at `docs/escalation/BLOCKER.md` and `docs/escalation/CCR.md`), with a stated response-commitment table. Cadence: daily/weekly/monthly/quarterly integrator duties (§8). Charter tasks `L0-00-01`..`L0-00-09`: establish branch topology and root CODEOWNERS; publish the charter; author the machine-readable partition map; author the lane guard and Makefile gates; author escalation templates; publish the L0 decision register; publish the cadence checklist; publish the merge-train runbook; rehearse the merge train on empty lane branches.
