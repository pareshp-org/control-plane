# Product P4 workflow — PLACEHOLDER
# Replace Product-P4 with the real product name before Phase 1 (FD-030/PFD-030)
# See product-template.yml for the full template
name: product-P4

on:
  push:
    branches: [main]
    paths:
      - "records/products/P4/**"
  workflow_dispatch:

jobs:
  ci:
    name: CI — Product P4 (placeholder)
    uses: ./.github/workflows/reusable-ci.yml
    with:
      product_id: "P4"
      environment: "ci"
    secrets:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
