# Product P3 workflow — PLACEHOLDER
# Replace Product-P3 with the real product name before Phase 1 (FD-030/PFD-030)
# See product-template.yml for the full template
name: product-P3

on:
  push:
    branches: [main]
    paths:
      - "records/products/P3/**"
  workflow_dispatch:

jobs:
  ci:
    name: CI — Product P3 (placeholder)
    uses: ./.github/workflows/reusable-ci.yml
    with:
      product_id: "P3"
      environment: "ci"
    secrets:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
