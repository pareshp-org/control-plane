# Product P1 workflow — PLACEHOLDER
# Replace Product-P1 with the real product name before Phase 1 (FD-030/PFD-030)
# See product-template.yml for the full template
name: product-P1

on:
  push:
    branches: [main]
    paths:
      - "records/products/P1/**"
  workflow_dispatch:

jobs:
  ci:
    name: CI — Product P1 (placeholder)
    uses: ./.github/workflows/reusable-ci.yml
    with:
      product_id: "P1"
      environment: "ci"
    secrets:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
